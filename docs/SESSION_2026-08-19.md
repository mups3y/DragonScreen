# SESSION 2026-08-19 — coast retrograde, rendezvous warp-then-orient, de-orbit aim-at-LZ

DLL `758f490a452233ced06ae30abda76c98`, installed, **UNFLOWN** (user will test). All three fixes are
the user's own stated procedures — implemented verbatim, grounded in `flight_0819_115518/120219`
(now archived), not guessed.

## 1. Booster COAST — pure surface retrograde, no lean (`pure/Landing.cs` ~795)
User: "after boost back burn you command all kinds of AOA changes when all you really have to do is
point retrograde (surface not orbital) and wait for the atmosphere braking burn."
- The COAST-DESCENDING branch set `c.GuidedLean = true`, which armed the AOA/lean law in
  `BoosterRecovery.Aim` (~1421). flight_0819 COAST: attErr avg **39.5°/max 131.5°**, actP 29.6% actY
  28.8% — the wagging the user saw. Roll **319°** (F9I ~102).
- FIX: **dropped `GuidedLean` from the coast branch.** `LandingAim.SurfaceRetrograde` already aims at
  `-v.srf_velocity` (surface, not orbital), roll already locked (`upHint = zero`). The guided lean that
  steers toward the pad now belongs only to DESCENT (after the entry burn), where there is air to steer
  in. Expectation: coast attErr → near 0, and the roll excursion should shrink (no pitch/yaw lean
  cross-coupling into the weak roll axis). Roll is still the known kinematic coast issue — verify.

## 2. RENDEZVOUS — warp to within 10 min, THEN orient, THEN warp to node (user procedure)
User: "you plot a perfect intercept. However you then move into position for the next manoeuvre node
(RCS, ruins the plotted intercept) and then warp to the node. Instead: warp to within 10 minutes of the
node and only then point the right way for the burn. From there warp to the manoeuvre and complete the
burn. You are also warping past the closest approach point."

**Root cause (from the propellant ledger, not a guess):** flight_0819 **ran the tank DRY** (195→0)
before de-orbit — de-orbit owner had 0 mono. `RideIntercept`/`MatchAltitude` handed the node to
`NodeExecutor` only ~60 s out (`Approach.LeadS`); with `BurnExec.AlignRcsBelowS = 45`, a turn that big
inside 45 s **buys RCS every node**. PhaseDownOps already hands off ~30 min out and turns on wheels for
free — the intercept/altitude legs did not.

**Fix:**
- `NodeExecutor`: new `OrientLeadS = 600` + `WarpToOrient`. While `Phase==Aligning` and ignition is
  more than 10 min away, the controller is **released** (no steering, no RCS) and the coast is warped to
  `ignitionUt − 600`. Only inside the window does `Align()` turn the ship — minutes of wheel authority,
  `NeedRcsToAlign` false → no bought RCS. No one-orbit refusal on this warp (the caller bounds the node
  time; an intercept is ≤ `CaSpanPeriods` = 1.6 orbits out). Then Hold warps the last ≤10 min to the
  node, then Burn. Warp targets are the orient point and ignition−6 s, both **strictly before** closest
  approach → cannot warp past the pass.
- `StationApproach.RideIntercept` and `MatchAltitude`: **hand the node to `NodeExecutor.Begin` at plot
  time** and deleted their own hold/warp state machines (`matchUt/matchWarpAsked/matchDistM`,
  `altBurnUt/altWarpAsked` removed). The Δv is computed at the pass/crossing from the CURRENT orbits and
  nothing burns before then, so up-front is exact (frame-correct `.xzy` preserved). `NodeExecutor` now
  owns the whole warp→orient→warp→burn for every node.
- Cosmetic quirk left as-is: while any node is active `MatchAltitude`'s top guard labels the note
  "ALTITUDE MATCH" even for an intercept node. Harmless — `NodeExecutor.Tick` (FlightDriver:119) drives
  the burn independently.

## 3. DE-ORBIT — aim the burn's drag-aware impact directly at the LZ (built this session, from prior edits)
flight_0819: entry loop now CLOSED (`MarginScale = 0`, `liftMin −1.00`, actively shortening) → splashdown
**37 km** (was 74.7). Still long because the BURN stopped on its old floor leaving 37 km for entry to
reel in. Now installed:
- `DeorbitOps.Tick`: aim scan uses **drag-aware** `ImpactPredictor.Predict(ship, CapsuleBcKgM2=440)` vs
  the LZ, not the vacuum solve (vacuum read 348 km long while drag-aware read the real ~75 km).
- `DeorbitBurn.PeriapsisTargetM` −31800 → **−70000** (hard safety backstop only; objective is
  `AimMissM < LzToleranceM`).
- `EntryMargin.MarginScale = 0` (aim predicted impact AT the LZ; steer only nulls drift).
Expectation: the burn drives its own drag-aware impact onto the LZ and stops there; entry holds it.
Verify actual splashdown miss and that mono is NOT dry at de-orbit (fix 2 should leave a full-ish tank).

## Verify next flight
- COAST: attErr ~0, roll ↓ from 319.
- Rendezvous: mono NOT exhausted (was dry); each node = "warp → orient on wheels → warp → burn"; arrives
  co-orbital; no warp past closest approach.
- De-orbit: splashdown miss small (was 37 km); de-orbit tank not dry.
- Tests green, audit WRONG 0, md5 installed==built — all confirmed at build time.

## Still open (NOT this session)
Dead adaptive de-orbit code (`DeorbitPoint`, `AdaptiveIgnition`, `PredictFromState`) — uncalled, delete
next pass. Docking TOP axis INVERTED (flight_0819). `CONTENDED:deorbit` two-controller rows. TO-DO:
docking KSP navball + markers/speed/mode; NAV orbit page → KSP map.
