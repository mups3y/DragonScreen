# Flights excluded from the tuning aggregate (genuinely BROKEN control — sustained saturation / mispoint).
# One filename-substring per line; '#' comments. Re-run tools/tuning_db.py after editing. Does not delete files.
# NOTE: the retrograde-plane flights (post-fix, clean CONTROL but wrong GUIDANCE/plane) are NOT excluded here —
# their attitude-loop/authority data is clean (pe_p95 ~0.08°); only their plane/inc/RAAN is contaminated. Exclude
# them too before tuning GUIDANCE, but keep them for CONTROL-AUTHORITY tuning.
20260827_041213   # S2 sat_duty 0.56 — RCS-fighting, sustained saturation
20260827_053613   # S2 sat_duty 0.55 — RCS-fighting
20260827_030705   # S2 pe_p95 8.3  — sustained mispoint
20260826_231747   # early RUD flight (pre-fixes)
20260826_235215   # early RUD flight (pre-fixes)
