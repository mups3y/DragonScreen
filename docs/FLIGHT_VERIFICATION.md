# FLIGHT VERIFICATION — evidence log (ACTIVE)

> The record of what has actually been verified, at which level, with evidence. Governed by `MASTER_BUILD_SPEC.md`. A capability is only as proven as its highest **flight** evidence — headless green is L1, never flight proof (rule V1). Every significant flight gets an ID and a row here.

## Verification levels
L1 software/math (pure tests) · L2 KSP integration · L3 single-vessel flight · L4 multi-vessel flight · L5 end-to-end mission.

## Flight-ID scheme
`DS-<CAMPAIGN>-<NNN>` — campaigns: `ASC` ascent · `ABORT` launch escape · `RTLS`/`ASDS` booster · `RV` rendezvous · `DOCK` docking · `RET` return · `E2E` full mission · `DOCKUI` docking-screen UI. Record: build/commit, config, mods, objective, start conditions, result, failure point, suspected root cause, evidence (recorder path).

## Baseline
| Date | Commit | Item | Result |
|---|---|---|---|
| 2026-08-31 | `9073429` | `python plugin/build.py test` (headless L1) | **PASS**, exit 0. 26 unit suites (~515 checks) + Tier-2 dispersion 731,239 checks (100k property cases: control/rendezvous/docking/return/fdir). 0 failed. |

## Protected flight-proof registry (invalidated by touching the listed code, per rule V4)
| Capability | Level | Status | Evidence | Invalidated by changes to |
|---|---|---|---|---|
| **Ascent to orbit (S1→MECO→sep→S2→SECO→orbit)** | **L3** | ✅ **FLIGHT-PROVEN** (2026-08-31, units-fix build `f1a0cbb`) | **`DS-ASC-003`**: reached **194×403 km / 51.6°**; S2 `ctrl_tq_pitch` = 526 (units fix live, was 62,000); attitude tracking clean (S2 rates 0.1–0.4 dps, actuation ~0). Supersedes the `DS-ASC-001/002` S2 tumble. | `AscentControl`, `FlightDriver`, `AttitudeController.ControlTorque`, staging/sep, `MissionConductor` |
| Max-Q launch escape (attitude authority, geometric RCS fallback, chutes, splashdown, crew survival) | L3 | FLIGHT-PROVEN | prior audit | `AbortControl`, `FlightDriver`, abort latch, RCS authority |
| Dual-vessel booster control (Dragon active + non-active booster on own `OnFlyByWire`) | L4 | PARTIALLY PROVEN (controlled, not landed) | prior audit (~16k OnFlyByWire callbacks) | `MissionConductor`, `BoosterControl`, `RangeExtender`, `FlightDriver` |

> **Re-fly requirement (ACTIVE — the three rows above are currently INVALIDATED, rule V4):** Phase-2 landed as CODE — UNFLIGHTED on 2026-08-31 (commit pending): the AuthorityManager extraction + the `Clamp1`/`Clamp1f`/`Clamp01f` NaN fix touch `FlightDriver` and `BoosterControl`. The extraction is **behavior-preserving by construction** (the proven `OnFlyByWire` actuation path is unchanged; only additive read-only authority publishing + NaN guards were added), so the flights are expected to reproduce the prior results — but they are not re-proven until flown.
>
> **Gate before Phase 3 — status 2026-08-31 (PARTIAL, see flight log `DS-ASC-001`):**
> - `DS-RTLS/dual` — **re-confirmed** (controlled, not landed): Dragon active + non-active booster on its own `OnFlyByWire`, both held loaded/unpacked, no authority loss. ✓ *as flown*
> - `DS-ASC` (ascent to orbit) — ✅ **DONE (`DS-ASC-003`, 2026-08-31):** reached 194×403 km / 51.6° on the units-fix build; S2 `ctrl_tq` 526 (was 62,000), tracking clean. The ascent L3 proof is **restored**.
> - `DS-ABORT` (max-Q launch escape → chutes → splashdown, crew survive) — **STILL OWED** (not exercised). Also re-invalidated by the `ControlTorque` units fix (`f1a0cbb`) — the abort RCS authority path changed.
> - `DS-RV/DOCK` (rendezvous → dock) — **BLOCKED by a fuel-budget bug:** the far-field TRANSFER burn drained ~85% of MMH (see `DS-ASC-003` below), leaving nothing for terminal approach/dock.
>
> Install with `python plugin/build.py install` (close KSP **and CKAN** first; a DLL change needs a full game restart).

## Open (no flight proof yet)
Rendezvous (reaches region, no dock) · Docking (no end-to-end) · Return: deorbit→entry→splashdown (**0 autonomous returns to date**) · Booster RTLS landing · Booster ASDS landing · Booster recovery ignition reliability · RCS-depletion handling · RSS entry targeting · water-site scan.

## Return-history reconciliation (Phase 1 — DONE)
User signal "12 killed / 17 rescued by hand / 0 autonomously returned" is now **CORROBORATED by two independent repo sources**:
- `RETURN_FIX_PLAN.md` §Why (doc removed 2026-08-31; preserved in git history) — the KSP-log ledger from Chris's 2026-08-30 return-test session: **12 killed (respawned), 17 rescued by hand, 0 brought home by the autopilot.**
- `dashboard/klm_data.json` — partial audit (since 2026-08-28, 10 flights via `dashboard/audit_kerbals.py`): `home: 0`, `rescued: 0`, `died: 8`, `stranded: 8`, `returning: 16`. Independent confirmation of **0 autonomous returns**.

**Firm fact:** RETURN = OPEN, **0 autonomous returns to date** (two sources). Exact kill/rescue tallies vary by accounting window (KSP-log session ledger vs recorder-CSV scoreboard); the operative signal — the autopilot cannot yet bring the crew home — is confirmed. Return is Phase 14; not touched this session.

## Flight log

### ⚠ DS-ASC-006 — trust-stock fix is LIVE but INSUFFICIENT; the two real reasons are procedure + constant pulsing — 2026-08-31
- **Build:** trust-stock (`305ef2c`). **Evidence:** `Crew-2_20260831_204829.csv` (7394 rows) + `KSP.log`. Detumble only **2.9 dps** (was 34 — calmer).
- **✅ Fix confirmed live:** capsule `ctrl_tq_yaw` 2.1 / pitch 2.7 (was 10.3 / 13.5) — the geometric Max is gone. **But it barely helped:** applied attitude duty 59%→**51%**, fuel still **94% attitude**, min range 90→**58 km**, still ran dry (~92 km), **no dock**. So the authority over-read was NOT the dominant cause — I over-invested in the estimate.
- **⛔ THE TWO REAL REASONS (owner-identified, data-confirmed):** **(1) constant thruster pulsing** — attitude firing is **94% during `Phasing`** (tight continuous prograde hold; zero big att_err jumps → not chasing, just holding to ~0 with no wide deadband) vs 18% in Coast. **(2) wrong rendezvous procedure** — a crude continuous far-field Hohmann → CW-at-100 km, not the real named-burn co-elliptic sequence; range stalls ~93 km, never reaches CW/dock. Both are in `docs/PHASE_3_RENDEZVOUS_RESEARCH.md` (real Dragon = discrete named burns + **free drift** on a co-elliptic orbit ~10 km below → AI at **7.5 km** → CW two-impulse to WP0/1/2 → dock).
- **→ REBUILD PLANNED (not implemented):** `docs/RENDEZVOUS_REBUILD_PLAN.md` — discrete named burns + co-elliptic drift (fixes constant pulsing) + CW terminal from 7.5 km with R-bar/V-bar waypoints (fixes the procedure). Review-gated; biggest risk = the Δv budget at 21% efficiency (L1 budget test first).

### ✅ DS-ASC-005 — instrumented re-fly RESOLVES the terminal drain: yaw authority over-read ~4×, attitude ~85% of the fuel — 2026-08-31
- **Build:** instrumentation (`app_*`, `rcs_pulse_att/trans`, pulse-config log). **Evidence:** `Crew-2_20260831_194036.csv` (5200 rows) + `KSP.log`. Same terminal drain as DS-ASC-004 (mmh 0.90→0.02 over ~530 s at ~90–130 km).
- **✅ Pulse config PROVEN in-flight (finding 4a):** `KSP.log` — `UseRcsPulse=True deadband=0.050 minOn=0.060 minOff=0.060 full=0.900`. `rcs_pulse_att=1` throughout the terminal → **PWPF was active during the drain.**
- **✅ REQUESTED vs APPLIED (the correct technique; retracts the DS-ASC-004 demand-based read):** attitude demand `|act_*|>0.05` = **97%** duty, but the **APPLIED** (post-pulse) firing `|app_*|≠0` = **59%** — PWPF cut ~40% of the demand. Translation applied = 19%. So PWPF works; 59% applied attitude is still the drain. Yaw leads (app_yaw 40%, app_roll 29%, app_pitch 24%); yaw demand saturates ≥0.90 for **26%** of ticks (those bypass PWPF as continuous).
- **✅ FUEL ATTRIBUTION (RCS impulse by applied category):** att_only 60% / both 26% / trans_only 8% / neither 7% → **attitude‑driven ≈ 76–85%, translation ≈ 8–16%.** The terminal drain is overwhelmingly ATTITUDE.
- **✅ ROOT CAUSE — YAW AUTHORITY OVER-READ ≈ 4.1× (regression from APPLIED command):** net torque (`MOI·Δω`) vs `app_yaw` gives DELIVERED per-unit authority **K=2.5** vs the loop's `ctrl_tq_yaw` estimate **10.3** (n=1711). The loop believes ~4× the yaw authority it delivers → commands ~2× too-aggressive `targetOmega` → saturates → the RCS fires at 59% applied duty → 85% of the terminal MMH burns on attitude. This is the **"secondary over-count" residual** deferred at the units fix (the geometric over-counts *achievable* RCS torque on the asymmetric capsule) — now measured at ~4× for the gimballess capsule, and the dominant terminal-fuel driver. (Consider also the per-phase `CapsuleRcs.*Pct` scaling as a possible component of the 4× — confirm in the fix.)
- **✅ FIX IMPLEMENTED (owner-directed, touches proven `AttitudeController.ControlTorque`, rule V4):** **use STOCK `GetPotentialTorque`; DROP the geometric `Max`.** Per-axis (applied regression + capsule dump): delivered pitch 0.6 / roll 1.3 / yaw 2.5 kN·m; stock 2.2 / 3.1 / 1.3 (0.5–3.7× of delivered); geometric 13.5 / 10.7 / 10.3 (**4–22× over**, and it won the old Max). Trusting stock cuts the over-read from 4–22× to 0.5–3.7× → the loop stops over-driving the gimballess capsule. S2 stays correct (gimbal 464 + RCS-stock ~8 ≈ delivered 445). Geometric still computed for `rcs_geo_*` diagnostics. **This reverts to the very first "trust stock" fix I later dropped — I'd dropped it on a PRE-PULSE-demand regression (invalid technique); DS-ASC-005's applied-actuation regression vindicates it.** **L1 proof:** `AttitudeLoopTest` — actuator effort (∫|act|) from a 10° hold is >2× higher with the geometric (pitch geo 25 vs stock 8; yaw geo 4.3 vs stock 1.6). PWPF + the C guard stay; no deadband change. **RE-FLY owed** (ascent/abort/booster/deorbit + rendezvous-to-dock). **Residual:** stock still slightly over-reads pitch (2.2 vs 0.6) — if the re-fly still drains, a per-axis achievability refinement is the next step.

### ⚠ DS-ASC-004 — A1 works, rendezvous still runs dry; terminal-drain MECHANISM is UNKNOWN — EVIDENCE REQUIRED — 2026-08-31
> **RETRACTION (2026-08-31 verification, reviewer-prompted).** My first DS-ASC-004 read — "terminal ATTITUDE limit cycle because the loop has no deadband/PWPF" — is **WRONG / UNPROVEN** and is retracted. `plugin/src/FlightDriver.cs` **already has a PWPF/deadband path** (`UseRcsPulse=true` default, `RcsPulseDeadband=0.05`, `RcsPulseMinOn/Off=0.06`, `RcsPulse.Step` on pitch/yaw/roll when the engine is off — L170-203), implemented + tested in `pure/RcsPulse.cs` + `test/RcsPulseTest.cs`. My "no deadband" claim read only `AttitudeLoop.cs:27` (the PID's *internal* deadband) and missed this downstream stage. Worse: the recorded `act_*`/`trans_*` are **pre-pulse controller DEMAND** (`FlightLog.cs:100` logs `AttitudePilot.Act*`; `FlightDriver.CmdTrans*` L134) — the applied post-pulse actuation is NOT recorded — so my "68–82% duty" measured demand, **not delivered RCS firing.** Also unaccounted-for: the per-phase capsule-RCS scaling (`CapsuleRcs.AttitudePct=40 / ApproachPct=30 / DockPct=20`, `tuning.reference.cfg`; no `tuning.cfg` override → these applied in-game) and `Attitude.CoastMaxRateDps=4`.
- **Build:** `f1a0cbb`+A1+C guard. **Evidence:** `Crew-2_20260831_170204.csv` (5694 rows, MET 0→85,241 s ≈ 23.7 h warp) + `KSP.log` + dumps `geometry_dump_{pad,manual_0s}`. ISS locked as target (**417×421 km**, log).
- **✅ A1 CONFIRMED:** inserted **366×363 km** (50 km below the 417 km station, not 200) — S2 `ctrl_tq` 532 (fix live). Far-field Transfer raised ap 367→405 km in ~50 s (small, as designed). Phased ~23 h cheaply (mmh 0.92→0.84). **The transfer is no longer the problem.**
- **✅ C guard CONFIRMED tripping** (log 17:17:22: "return prop 20% ≤ reserve 20% — translation INHIBITED") — **but it did NOT stop the drain**, because the drain is **attitude, not translation.**
- **What IS established (unretracted):** A1 worked (insertion 366×363, small transfer); the tank still ran dry in the terminal phase — mmh crashed **0.84→0.02 in ~500 s** with `rcs_thrust_n` 12–28 kN (that column is the *delivered* total RCS thrust, `Actuator.RcsThrustN`); the C guard tripped at 20% (log) yet mmh continued to ~0.02. So the terminal drain is **real**. `rate_yaw` did swing ±4 dps around `att_err` ~3° (a genuine oscillation signature), and the pre-pulse yaw/roll DEMAND saturated often.
- **⛔ ROOT CAUSE — UNKNOWN — EVIDENCE REQUIRED.** The current recorder **cannot attribute** the drain: it logs pre-pulse demand (`act_*`/`trans_*`) + the *total* delivered `rcs_thrust_n` + *total* `mmh/nto_frac`, but NOT the post-pulse applied actuation, the pulse state, the PWPF config, or the attitude-vs-translation thrust/propellant split. Candidate contributors — controller demand, post-PWPF actuation, the ~1.5× authority over-read (`ctrl_tq_yaw` 10.3 vs a flight-measured ~7), translation, the CapsuleRcs phase scaling — **cannot be separated from this flight.**
  - **⚠ MY TECHNIQUE ERROR (owned, twice):** (1) the C guard was built for the previous (translation) drain without confirming the current path; (2) I then inferred delivered RCS duty from **pre-pulse controller demand** and asserted a limit cycle + "no PWPF", both wrong. Correct technique = trace the full chain (request → FlightDriver ownership → PWPF output → `FlightCtrlState` → RCS thrust/resource → measured response) with recorded evidence.
  - **✅ INSTRUMENTATION-ONLY change made (no control-law change):** `FlightDriver` now publishes the APPLIED post-pulse actuation + pulse-active flags (`AppliedPitch/Yaw/Roll/TransX/Y/Z`, `PulseAttActive/PulseTransActive`); recorder columns `app_pitch/yaw/roll`, `app_tx/ty/tz`, `rcs_pulse_att/trans` (`FlightRecorder.PutApplied`, wired in `FlightLog`). `RcsPulseTest` gains a case pinning that a *saturated* demand still fires near-continuous (PWPF does not throttle a full command). Headless L1 green; installed.
  - **OWED: one focused re-fly** (target set, run to terminal approach) to record requested-vs-applied + pulse state, THEN quantify each contributor. **No deadband/PWPF change, no `AttitudeLoop`/`ControlTorque`/tuning change until that flight proves the mechanism.** (34 dps detumble at phasing start = uncontrolled-coast drift, separate + self-corrected.)

### ✅ DS-ASC-003 — ascent to ORBIT (S2 units-fix flight-proven) + rendezvous fuel-exhaustion — 2026-08-31
- **Build:** `f1a0cbb` (the `ControlTorque` ×1000 units fix). **Config:** crewed Falcon 9 + Crew Dragon, RSS/RO, Cape; AUTO through ascent + rendezvous. `GeometryDump` (Alt+G) active. **Evidence:** `Crew-2_20260831_151611.csv` (4648 rows, MET 0→20,315 s ≈ 5.6 h with warp) + booster `Crew-2_Probe_20260831_151854.csv`; geometry dumps `geometry_dump_{pad,manual_0s,manual_226s}.csv`.
- **✅ ASCENT TO ORBIT — PROVEN.** Reached **ap 403.6 / pe 194.4 km, inc 51.64°** (real, stable orbit; both apses positive, SECO reached). The S2 tumble is GONE.
  - **Units fix confirmed LIVE:** S2 `ctrl_tq_pitch` median **526.6** (= gimbal 464 + RCS-geo 62, exactly as designed; was ~62,000) → `maxAlpha` 0.42 rad/s² (was 37.6). The recorder tell of the fix, seen in flight.
  - **Attitude tracking is clean:** during S2 the body rates are **0.1–0.4 dps** and `act_pitch` ≈ 0 — no oscillation, no saturation. The loop holds its commanded attitude well.
  - **The ~15° "att_err" during S2 is ANGLE OF ATTACK, not a tracking error** (`FlightRecorder` sets `att_err_deg = aoaDeg` during ascent, `FlightRecorder.cs:227`). It is the guidance thrust-vector steering the nose off-prograde for insertion; it jumps to ~18° at MVac ignition (a brief transient, rates −11/+10 dps for ~1 s) then bleeds to ~9° — i.e. guidance, plus a minor ignition transient. Ascent control quality is good; no instability to "trim." (If the user's observed "trim" item is something specific on the navball/screens, it needs to be named — the data shows clean tracking.)
- **⛔ RENDEZVOUS — RAN OUT OF MMH before terminal approach (no dock). ROOT CAUSE (data-established, NOT yet fixed):** the far-field **TRANSFER** burn (`Phasing.FarGuide`: raise ap toward the station, prograde, bounded — *intent is correct*) is grossly **propellant-inefficient**.
  - The burn (MET 18,577→18,805) raised **ap 200→406 km** (pe held ~197) — a ~**61 m/s** orbital maneuver by vis-viva — but consumed **1171 kg = ~85% of MMH** (`mmh_frac` 0.90→0.09; ~370 m/s by rocket-eq at Isp≈300). **~15–25% efficient.** The tank then emptied during `ApproachInit`; no fuel for velocity-match/dock.
  - **Why so wasteful:** it uses continuous low-thrust **Draco translation** (the Dragon has no main engine), which (a) cancels ~33% internally (`rcsbal_force_frac` = 0.67), and (b) spends the rest over a long continuous arc rather than an impulsive Δv — and there is **no propellant-budget guard** (`dv_planned_mps` is blank for all 1133 Phasing rows; the transfer burns purely on the "ap < target" condition). Attitude was well-held (att_err 2.6°), so this is an **actuation-efficiency / burn-strategy** problem, not a pointing one.
  - **INVESTIGATION CONCLUSION (2026-08-31, code + config + flight):** the loss is **inherent RCS-translation inefficiency**, not the phasing logic and not attitude pointing.
    - **Draco Isp = 240 s** (`TE_CD2_POD.cfg`, `atmosphereCurve key 0 240`; 2× `ModuleRCSFX`, 1.5 kN, MonoProp). So the 61 m/s ap-raise should cost **~205 kg** but cost **1171 kg → 21% efficient (~5× waste)**. Not a low-Isp artefact — a real efficiency loss.
    - **Mechanism:** the burn is a continuous full **`trans_z`** on the Dragon's 16 Dracos, which are **2 all-axis `ModuleRCSFX` blocks with only a per-module `thrustPercentage` — no per-thruster control** (`docs/RCS_BALANCE_FINDING.md`). KSP's naive "fire every thruster with a component along the demand" delivers a small **net** prograde force per unit gross thrust (much of it cancels/goes off-axis), and induces a large self-torque (`rcsbal_torque_naive` ≈ 8.5 kN·m, ~60% of the capsule's ~14 attitude authority) that the loop counter-fires (`act_roll` active 78% of ticks) — but attitude firing is the minority; **translation cancellation dominates** (`trans_z`=1.0 continuous). Pointing is fine (att_err/AoA 2.6°).
    - **Why it strands the mission:** at 100% efficiency the 61 m/s raise is ~17% of the MMH tank (affordable, dock reachable); at 21% it is ~85% → nothing left for velocity-match/dock. **The far-field phasing strategy (insert LOW+fast, then raise ap) implicitly assumes more Δv than a Draco-only vehicle has at this efficiency.**
  - **NOT FIXED — review-gated (touches the proven rendezvous FSM + the deferred RCS-balance decision).** Options weighed: **(A)** insert closer to the station altitude so the efficient ascent buys the climb; **(B)** direct force-injection efficient translation (invasive, deferred by `RCS_BALANCE_FINDING`); **(C)** a propellant-budget guard (prevents stranding, doesn't enable dock alone); **(D)** a lower-Δv phasing strategy.
  - **✅ BUDGET SIZED (2026-08-31, measured + analytic; A1 validated).** Dragon full-MMH useful Δv = **66 m/s** (Isp 240, 21% eff, mass 9940→8707 kg). Required Δv by insertion margin below the station (transfer + null-relV-at-handoff + ~15 m/s braking, all @21%): margin **207 km (current) → 140 m/s ≫ 66 → strands** (the model reproduces the flight); **50 km → 49 m/s ✅** (4.0°/orbit phasing); 30 km → 37.6 ✅ (2.4°/orbit); 10 km → 37.6 ✅ but 0.8°/orbit (too slow). **Recommended margin ≈ 50 km** (fits with headroom, phases ~4°/orbit). Key assumption: 21% applies to terminal burns too (measured for the prograde transfer; confirm per-axis in re-fly — if lower, use a smaller margin, slower phasing).
  - **✅ DIRECTION CHOSEN (owner, 2026-08-31): (A) insert closer — the ascent delivers the Dragon near the station altitude (~416 km here; `parkAltM` = station − `CoEllipticBelowM` 10 km), so the far-field Hohmann injection (`AscentControl.cs:152` currently inserts CIRCULAR at `mission.ApoKm`=200 km) shrinks toward 0.** Design sub-options for the plan: **A1** insert circular a tunable margin *below* the station (small residual raise, affordable at 21%; phases slower — the trade to tune) vs **A2** insert elliptical (pe low for fast phasing, ap at station; needs ascent to target an ellipse + far-field phase-to-apoapsis). **Caveat to size in the plan:** the terminal CW approach/velocity-match/dock still run on 21%-efficient RCS, so the plan must budget those against the freed MMH. **✅ A1 IMPLEMENTED (2026-08-31, uncommitted→committing):** `AscentControl` now inserts `ParkBelowStationKm` (default 50) below the selected target's periapsis when a rendezvous target is set (`RendezvousParkAltM`, reads `v.targetObject`; guarded by same-body + `ParkInsertFloorKm` 185 km; falls back to the 200 km default with no target). The far-field logic is unchanged — its transfer just shrinks to the ~11 m/s residual. Headless L1 green (glue-only change, no unit-testable path). **⚠ RE-FLY REQUIRED (rule V4 — changes the flight-proven ascent target):** (1) ascent to the new higher circular insertion (~367 km) — confirm no g-limit/guidance regression; (2) rendezvous — confirm it phases (~4°/orbit, ~3–6 days warp), transfers cheaply, and reaches CW/dock within the 66 m/s budget; measure the terminal per-axis RCS efficiency (the budget's key assumption).
  - **✅ C (safety net) IMPLEMENTED (2026-08-31):** every rendezvous translation now goes through `RendezvousControl.RvTranslate`, which INHIBITS the burn + HOLDS + warns once the return propellant (`DockedSide.ReturnFraction`, worst-case MMH/NTO) falls to `RvReturnReserveFrac` (default **0.20**). Prevents the DS-ASC-003 total drain (0% = no attitude control = dead crew) and holds back a return-attempt margin. **Honest limit:** at 21% efficiency a full dock AND a full return do not both fit — the floor is a deliberate *catastrophe-preventer, not a return guarantee*, kept modest so it doesn't block a within-budget A1 dock; **size it to the measured deorbit cost on re-fly.** Headless L1 green. Re-fly to confirm it doesn't false-trip a healthy A1 rendezvous.

### ⛔ DS-ASC-001 + DS-ASC-002 — ascent FAILS at S2 (upper-stage attitude divergence) — 2026-08-31
> **CRITICAL, REPRODUCED IN BOTH POST-PHASE-2 RE-FLIES. This supersedes an earlier mis-assessment that called the ascent "nominal" — it is not.** First-stage ascent to MECO is clean, but **at S2 (MVac) ignition the upper-stage attitude control diverges and the S2+Dragon tumbles in pitch/yaw**, the S2 thrust is misdirected, apoapsis stalls and the vehicle **never reaches orbit**. Ascent-to-orbit is currently **BROKEN**.

- **Flights / recorders:**
  - `DS-ASC-001` — `Crew-2_20260831_094132.csv` (1763 rows) + booster `Crew-2_Probe_20260831_094424.csv`. DLL at HEAD `e1f5763` + uncommitted docking rebuild.
  - `DS-ASC-002` — `Crew-2_20260831_102133.csv` (1688 rows) + booster `Crew-2_Probe_20260831_102425.csv`. DLL = commit `f79402a` (docking rebuild + angular-rates publish; **no flight-code change vs 001**).
  - Config both: RSS/RO Earth, Cape, crewed (4), target ISS, **AUTO BOOSTER RECOVERY armed**. **Zero DragonScreen exceptions** either flight.
- **THE FAILURE (evidence — recorder `att_err_deg`, `rate_pitch/yaw_dps`, matched frame-by-frame to the separation screenshots):**
  - Ascent to MECO **and through the coast**: `att_err` ≈ 0–2°, body rates <1°/s, roll ~0° — **rock-solid** (S1 control path is fine; `102417`/`102426`/`102427` show Roll ≈0.0° at MECO).
  - **ONSET IS AT S2 IGNITION, NOT SEPARATION.** MECO (met 140) + decouple are clean and controlled; the vehicle coasts controlled (met 141–142, att_err 2°). **The instant the MVac lights (met 142–143) a pitch rate of ~−12°/s appears and is never arrested** — att_err ramps nearly linearly 3°→62° over ~5 s (`102429`/`102430`: HDG 043°→023°, Pitch 29°→55°, Roll 0°→−17° right as thrust comes on), then becomes a **divergent limit-cycle**: att_err oscillates ~24°↔124°, pitch rate ±40–68°/s, yaw rate building to −86°/s, HDG swinging wildly (043→003→314→019→107→231→143°) to the revert (met 163). **Roll stays ~0–2° (the roll-trim RCS holds it); the runaway is in pitch/yaw.**
  - **The BOOSTER diverges at the SAME TIME** (att_err 2°→141° over t0–15 s, engines correctly unlit). Both the active Dragon/S2 and the non-active booster lose attitude control simultaneously at separation/ignition → **points at a shared attitude-control-path cause, not an S2-gimbal-specific one.**
  - **Apoapsis frozen at ~120 km, velocity bleeding off** (S2 thrust misdirected); **perigee stays ≈ −6168 km (suborbital)** → **no SECO, no orbit.** User reverted at met 163 because it was visibly failing.
  - **FDIR did NOT detect it:** `fdir_fault`=None / `fdir_abort`=0 every row despite 100°+ error → fault spine **blind to attitude divergence**.
  - **Screens FUNCTIONED CORRECTLY throughout** (see `DS-DOCKUI`): all pages matched KER; the DOCKING angular-rates fix showed live values. The screens are NOT implicated in the failure.
- **The reverts were forced by this failure, not chosen** — both flights were reverted mid-S2 once the tumble made orbit impossible. Earlier note "ended by revert, deliberate, not a crash" was wrong.
- **✅ ROOT CAUSE — CONFIRMED headlessly (2026-08-31), evidence chain below. It is a control-authority OVER-READ of ~137× caused by a ×1000 UNITS BUG in the RCS geometric estimate (N·m vs the kN·m everything else uses), no disturbance.** (This corrects earlier wrong turns in this same investigation: NOT Phase-2/`Clamp1`; NOT a ~5× over-read; and NOT "the geometric over-counts unachievable torque 1000–7000×" — that framing was unit-confused. The dominant error is units; see the TRUE ROOT CAUSE bullet below.)
  - **Evidence 1 — flight regression (pure data, no model).** Regressing net torque (`MOI·Δω/Δt`) against commanded actuation over the S2 window gives, per axis: **slope K (real per-unit control torque) ≈ 451 (pitch) / 406 (yaw)**, and **intercept (disturbance torque) ≈ 0**. So (a) there is **NO external disturbance**, and (b) the **real** S2 pitch/yaw control authority is only **~451** — i.e. the gimbal alone; the RCS (loop estimate ~62,000) delivers **≈0 effective torque** despite its geometric estimate. (So the recorder's ~480 gimbal reading is the REAL authority, not an under-report — my earlier "artifact" note used an inconsistent unit comparison and is retracted.)
  - **Evidence 2 — the loop over-reads by ~137×.** The loop uses `controlTorque ≈ 62,000` (dominated by the RCS geometric estimate) while the real is ~451 → `maxAlpha = ct/MOI` is ~137× too high (S2 ≈ 37 vs a real ≈ 0.27 rad/s²). Origin: `c42ed20` (Campaign-6) credits the RCS geometric authority whenever the RCS master is ON; `8225df7` (roll-trim + ullage) arms RCS in S2 — breaking the `AttitudeController.ControlTorque` L195-198 assumption *"during gimbal-only ascent RCS is off."* First flown 2026-08-31.
  - **Evidence 3 — headless sim reproduces the tumble, no disturbance (`AttitudeLoopTest`, faithful port of `AttitudeLoop.Axis`+`Pid2`).** With MOI 1650, real authority 451, estimate 62,000, **D=0**: the loop **LIMIT-CYCLES** (matches the flight: starts small → grows → swings past 200°; commanded rate peaks ~7.4 rad/s vs the flight's ~7.6). It is a **threshold instability**: converges for over-reads ≲4×, limit-cycles past ~10× (the flight is 137×). A correct estimate (est = real) **CONVERGES** = fix preview. This is why my earlier stand-in (real≈11846, only 5× over-read) wrongly looked stable.
  - **✅ TRUE ROOT CAUSE — a ×1000 UNITS BUG in the RCS geometric estimate (CONFIRMED 2026-08-31 with real geometry + a units decomposition; this SUPERSEDES my earlier "the geometric over-counts unachievable torque 1000–7000×" framing, which was unit-confused and is retracted).** `ControlTorque` computed each thruster's power as `thrusterPower * 1000` (→ **N**), so the whole geometric estimate came out in **N·m**, while stock `GetPotentialTorque`, the gimbal report, and the MOI divisor are all in **kN·m / t·m²**. `MAX(stock[kN·m], geometric[N·m])` therefore ALWAYS picked the geometric — a number **1000× too large** — and `maxAlpha = ct/MOI` inherited the ×1000. Verified numerically:
    - **Units confirmed:** full-stack `moi_pitch` = 120,448 (t·m² — physical for a ~500 t stack; kg·m² would be 10⁸); S1 gimbal `ctrl_tq` 9,765 kN·m → maxAlpha 0.08 rad/s² (matches a hand-figured Merlin gimbal). So the stock/gimbal/MOI chain is correct in kN·m; the geometric was the lone N·m outlier.
    - **S2 decomposition (the decisive one):** `ctrl_tq_pitch` 62,465 − `rcs_geo_pitch` 62,000 = **gimbal `gx` = 464 kN·m**, which MATCHES the flight-**DELIVERED 445** (regression). So the **stock gimbal report is ACCURATE**; the delivered 445 is the gimbal, and the S2 RCS delivers ≈0. The 62,000 is purely the N·m-bugged RCS geometric (62 kN·m in correct units). maxAlpha read **37.6** rad/s² vs a real **0.27** → the tumble.
  - **Deorbit gate — RESOLVED (2026-08-31, capsule dump `geometry_dump_manual_2500s.csv` + flight `Crew-2_20260831_141924.csv`).** Dragon capsule alone (6.8 t, 16 Dracos, no gimbal): stock RCS **2**, N·m-bugged geometric **12,870** (→ **12.9 kN·m** fixed), flight-**DELIVERED 7 kN·m** (regression, autopilot run, n=832). Same ×1000 bug → maxAlpha read **919** rad/s² (real 0.5) → the **capsule spun under autopilot** exactly as observed. Campaign-6's premise ("stock under-reads ~2 vs a real ~12–20 kN·m") was **wrong about the number** (real is 7, not 12–20k) but **right about the direction** — stock (2) does under-read the capsule RCS ~3.5×, and the units-fixed geometric (12.9) floors it *closer*. So the geometric APPROACH is sound; only its UNITS were broken.
  - **The 8-possibility verdict (updated):** (5) **estimator over-credits — YES, via the ×1000 units bug** (dominant). (3) geometry yields low achievable RCS torque — **PARTIAL/secondary** (S2 RCS geometric 62 kN·m vs delivered ~0 — a real but harmless over-count, masked by the 464 gimbal). (1)(2)(4)(6) **NO**, (7) recorder **PARTIAL** (logged the geometric but not the delivered torque/stock split → the over-count was invisible from the CSV alone). **⇒ #5 (units), with #3 secondary.**
  - **✅ FIX — IMPLEMENTED 2026-08-31 (`AttitudeController.ControlTorque`): compute geometric power in kN (drop the `*1000`), keeping `MAX(stock, geometric)`.** Not a phase rule, not a slew clamp; roll-trim + tuning untouched (rule-compliant, smallest root-cause change). Preserves Campaign-6's geometric floor (per the directive) and does NOT assume the RCS is ineffective (credits the capsule RCS 12.9 ≈ real 7). **L1 proof (`AttitudeLoopTest`, closed-loop port of `AttitudeLoop.Axis`):** as-flown S2 (est 62,000) and capsule (est 12,870) LIMIT-CYCLE; the units-fixed estimates (S2 526 = gimbal 464 + RCS 62; capsule 12.9) both CONVERGE. The recorder's `rcs_geo_*`/`GeoTorque*` diagnostics are now in kN·m too (were N·m).
  - **KNOWN RESIDUAL (secondary, deferred — does NOT destabilise):** even in correct units the geometric over-counts *achievable* RCS torque for asymmetric layouts (S2 RCS 62 vs delivered ~0; harmless — the 464 gimbal dominates). A proper achievability cap (MechJeb-style translation-cancellation, or cross-check/cap) is future work, not required to stop the tumble.
  - **⚠ RE-FLY REQUIRED (rule V4 — flight-proven `ControlTorque` changed):** ascent-to-orbit/SECO, max-Q abort, dual-vessel booster recovery, AND a deorbit/prox-ops hold. Headless (L1) is proven; in-flight (L3) is NOT yet re-verified.
  - **INVESTIGATION TRAIL (kept so the dead ends are not re-walked):** ruled out — `Clamp1` NaN (commands finite); a disturbance torque (regression intercept ≈ 0); a ~5× over-read (stable — I'd wrongly sized the real authority at the S1-gimbal ~11846); B4 lead-comp vs an instant RCS (converges). The gimbal is fine (`gimbalRange=2°`, active); the low ~451 IS its real authority, not an artifact. What actually holds up: the ~137× authority over-read above.
- **What DID work (verified):** liftoff/clamp release; **PRE holds booster + S2 loaded/unpacked** (dual-vessel physics); ascent-to-MECO guidance (plane-locked, max-Q 31.8 kPa, g-capped 3.500); **MECO shut-then-decouple collision guard** → collision-free interstage separation; **C2 dual-vessel booster recovery** (Dragon active + booster on own `OnFlyByWire`, both alive to 3 km) → L4 control still functions; booster retrograde flip reached ~2° (but **underdamped**, oscillating 20–60°, and never lit its entry burn / not landed — reverted while still ascending). Screens rendered correctly incl. DOCKING (`DS-DOCKUI-001`).
- **Net verification impact:** **ascent-to-orbit is BROKEN (S2 tumble), reproduced twice — TOP blocker.** The Phase-2 re-fly did NOT clear the ascent proof; it **revealed a regression**. Protected "Ascent to orbit" row must be downgraded until root-caused and re-flown to SECO/orbit. Max-Q abort still un-exercised. Dual-vessel *control* re-confirmed (not landed).

### DS-DOCKUI-001/002 — DOCKING screen + all pages, in-game (L2) — 2026-08-31
- **All screen pages FUNCTION CORRECTLY** (verified across `DS-ASC-002`'s 75 in-flight screenshots, cross-checked to KerbalEngineer in-frame): FLIGHT (phase/abort-mode/gauges — abort mode escalates PAD→MODE 1-LOW ALT→MODE 2 correctly), VEHICLE OVERVIEW+MECH (ECLSS/net-power sign/accel decomposition), NAV GROUND-TRACK + 3D-PLANET (**perigee/time-to-pe correctly dashed on the suborbital arc** — the OrbitReadout guard works), DOCKING, SETTINGS CABIN/AUDIO/VIDEO(live interstage cam)/DISPLAY. Altitude/velocity/apogee/inclination match KER at every sampled point. **Zero DragonScreen exceptions.**
- **Rebuilt DOCKING** (real-HUD reticle-in-rings, **no navball**): owner verdict **"fine for now"**. Reads authoritative rel-nav; RANGE = KER target distance to the metre; X/Y/Z vector-sum = RANGE.
- **✅ Angular-rates fix CONFIRMED in-game (`DS-ASC-002`, commit `f79402a`):** the blue per-axis rate line now reads real values (e.g. `0.0 / 0.5 / 0.0 deg/s`) instead of "—".
- **Screen GAPS found:**
  - **No screen surfaced the S2 attitude-control failure.** Through the 100°+ tumble the chrome **STATE stayed NOMINAL** (FDIR blind, `fdir_fault`=None) and no page shows a GNC/attitude-error or guidance-tracking health readout. A catastrophic control divergence produced **zero crew alert**. (Ties to the FDIR finding in the DS-ASC entry; argues for a GNC/attitude-health element + FDIR attitude-divergence detection.)
  - Minor: FLIGHT reads PHASE **ASCENT while still clamped/​warping on the pad** pre-liftoff (AUTO arms the phase early). Phase-labeling nuance.
  - Still owed (known): target diamond on boresight (2-D bearing not yet published).

_(Older placeholder note removed — these are the first real entries this program.)_
