# RSS/RO Crew-2 build — session 2026-08-22

## ⛔ MISSION IS NOW CREW-2, not Crew-1 (user, 2026-08-22, from spacex.com)
Same vehicle class, pad, plane and recovery — so ALL ascent work carries over untouched. Crew-2 facts
(primary source spacex.com/CREW-2, per the no-Wikipedia rule):
- Launch **2021-04-23**, Falcon 9, **LC-39A** → ISS; autonomous docking ~23 h later (Apr 24).
- **Flight-proven hardware**: Dragon **Endeavour** (reused from Demo-2), booster **B1061** reused from
  **Crew-1** (literally the Crew-1 booster's 2nd flight). First crew mission on a flight-proven Dragon+F9.
- Return: splashdown **off Pensacola, FL** (Gulf) — the ONE functional delta vs Crew-1 (which was off
  Panama City). Retarget the de-orbit aim to Pensacola when we build the return; need the lat/lon.
- Crew: Kimbrough (CDR), McArthur (PLT), Hoshide (JAXA), Pesquet (ESA). Duration 199 d (we skip the stay).
- 51.6° to the ISS, droneship recovery, Harmony-forward autonomous dock — identical to Crew-1.
  ⇒ Ascent/azimuth/rendezvous/dock are unchanged; only the splashdown coordinate + metadata differ.

---


Continues the RSS/RO track opened 2026-08-21 (see `docs/SESSION_2026-08-21.md` §"RSS/RO Crew-1 track"
and the approved plan `.claude/plans/snoopy-orbiting-hennessy.md`). Goal: DragonScreen (NOT MechJeb)
flies the real Crew-1 mission in RSS/RO. Locked decisions: full DragonScreen guidance, no MechJeb;
Tundra + TundraExploration_RO; full fidelity minus the 167-day warp.

## Environment reality
- The RSS/RO stack is installed in the **main** KSP: `C:\Program Files (x86)\Steam\...\Kerbal Space
  Program` (KSP 1.12.5). It has BOTH DragonScreen AND RealismOverhaul/RealFuels/ROEngines/RSS(KSCSwitcher,
  RSS-CanaveralHD)/RP-1-ExpressInstall/Kopernicus/FAR/Tundra v7.2 + TundraExploration_RO + HabTech2.
  So `build.py install` puts the DLL into the same install now running RSS/RO.
- Save game: `saves/test`. Craft in `saves/test/Ships/VAB`.

## ⛔ THE FIX FILES (in the KSP install, NOT in the repo) — `GameData/zzz_TundraRO_Fixes/`
Three cfgs, all mine, all needed. If the station/rocket regress, check these first.
- `TundraRO_Fixes.cfg` — **B9 fix only** (final state). Removes the orphaned `TE_VolumeBias`/`ExtraVolume`
  B9PartSwitch from the Dragon capsules (pack forgets to, so it clashes with RealFuels ModuleFuelTanks).
  Earlier engine-uprate + tank-prefill hacks were REVERTED — they caused a broken 0-N Merlin and a
  phantom mass; RealFuels' own FILL button is the way for tanks, and Merlin1D++ (below) for thrust.
- `HabTech2_ROFix.cfg` — (A, :FIRST) tag every `ht2_*|ht_*|ScienceBox` `RSSROConfig=True` so RO's
  **HardRemoveNonRO** doesn't strip them; (B, :FINAL) `!rescaleFactor,*=DEL` + `%rescaleFactor=1.81`
  on all of them so the station is ONE uniform scale (RO natively sets Cupola=2 and leaves the modern
  USOS truss/solar/robotics parts uncovered → non-uniform → gaps).
- Also edited a stock RO file: `GameData/ROEngines/PatchManager/ActiveMMPatches/Merlin1.cfg` — its sole
  line (a `!PART:HAS[~name[ROE-*]&#engineType[Merlin1]&#category[Engine]]` cleanup) is **commented out**,
  because it was DELETING the RO-configured Tundra Merlin engines (Ghidorah S2 etc.) → "missing part
  TE.19.F9.S2.Engine". PatchManager may regenerate it; if the F9 loses engines again, re-comment it.

## RO gotchas learned (don't re-discover)
- **ROEngines deletes non-ROE Merlin engines** (Merlin1.cfg cleanup) — see above.
- **RO `HardRemoveNonRO` deletes any part lacking `RSSROConfig=True`** — bit both Tundra (partly) and
  HabTech2 (83 of 100 parts). Fix = tag them.
- **Tundra RO Falcon 9**: the octaweb config is the **Falcon 9 v1.1 Merlin** (~742 kN/engine vac,
  underpowered for Block 5). ROEngines already ships **Merlin1D+ (7425 kN) and Merlin1D++ (8227 kN ≈
  Block 5)** as SELECTABLE configs in the engine GUI — switch to Merlin1D++, don't hand-edit thrust.
  Real Block 5 (sourced): Merlin 1D 854 kN SL / 981 kN vac, 9 = 7607 kN SL; MVac 981 kN.
- **The Ghidorah "Erector" launch mount** is part of the craft and shows ~1000 t in VAB Vessel mass +
  drags TWR to ~0.4 — but it's a LAUNCH CLAMP, stays on the pad, doesn't fly. Ignore that mass; real
  flying rocket is ~540 t. (Cost me a long mass-hunt; the user spotted it.)
- **RealFuels tanks build EMPTY**; the Tundra F9 tank config assigns no propellant. Fill with the
  RealFuels tank GUI FILL/quick-fill (kerolox at the Merlin ratio). Config pre-fill is unreliable
  (stacked/phantom). Verified S1≈405 t, S2≈103 t → ~508 t, correct.
- **Scale mismatch clips pre-built craft**: RO rescales parts (F9 1.48×, HabTech2 1.81×) but saved
  craft positions are stock-scale → clipping. No mod converts craft to RO scale. Rebuild fresh (parts
  snap to RO nodes) OR run the position-rescale script (below) once scale is UNIFORM.

## The ISS + Dragon (built, verified in the save)
- **`ISS USOS Real Size`** — IN ORBIT, verified in `saves/test/persistent.sfs`: SMA 6,791,010 →
  **420.0 km circular, INC 51.600°, Earth (REF 1)**. Textbook Crew-1 target. Core part ht2.moduleUnity.
- **`Falcon 9 Crew Dragon 2 Real Size`** — craft file in VAB (launches for the mission).
- HabTech2 station was made loadable by `HabTech2_ROFix.cfg`, then the pre-built stock-scale USOS craft
  was rescaled ×1.81 by **`scratchpad/rescale_craft.py`** (scales `pos`/`attPos`/`attPos0` only; KSP
  places editor parts by `pos`) → saved as **`ISS USOS RO (rescaled 1.81)`**. Loaded aligned once scale
  was forced uniform (fix B). Dragon docks at **Harmony forward / IDA (B10.IDA)** — both Dragon and ISS
  use B10.IDA, so they latch.

## Ascent guidance — progress this session
- **FuelFlowSimulation ported** to `src/pure/mechjeblib/` (staged-Δv budget; PSG prerequisite). Proven
  vs the rocket equation. See `docs/MECHJEBLIB_PORT.md` (steps 1-3 DONE; 4-6 KSP-coupled, pending).
- **`src/pure/LaunchAzimuth.cs`** — DONE, 12 headless checks (`test/LaunchAzimuthTest.cs`). Solves the
  launch heading from target inclination + launch latitude, spin-corrected. Stock 0.133° from equator →
  ~90 (unchanged); Crew-1 51.6° from 28.6°N → 45.0 inertial / 42.8 ground.
- **Wired into `AutoPilot.EngageCore`**: on engage, reads the TARGET STATION's own `orbit.inclination`
  and sets `Target.HeadingDeg` via `LaunchAzimuth.GroundHeadingDeg`. Logs `launch azimuth N deg into
  the station's I deg plane`. Build green. Unflown (glue).
- **A steps 1+2 DONE — `AscentTarget.ForBody(profile, atmosphereDepthM, parkingAltitudeM)`** (pure,
  6 tests in `FlightTest`). Kerbin's own depth+altitude returns `Station(p)` byte-identical; Earth's
  140 km atmosphere stretches `StageAltM` (MECO apoapsis + first-stage pitch-over `denom=stageAlt*
  gain/100`) by the depth ratio (×2) and targets a real **200 km** parking orbit. The pitch SHAPE
  (`MecoAngleDeg`, `PitchGain`) is body-independent and untouched; the 2nd-stage pitch/Q/turn-end
  already read the LIVE `AtmosphereDepthM`, so they self-scale. `ReferenceAtmosphereDepthM=70 km`.
- **Glue picks it** (`AutoPilot.EngageCore`): `else if (v.mainBody.Radius > RssBodyRadiusM=1e6)` →
  `ForBody(profile, v.mainBody.atmosphereDepth, RssParkingAltitudeM=200 km)`; else `Station(profile)`.
  Stock Kerbin path is EXACTLY as before. Crew-1 base profile = **Droneship** (barge recovery).
- So the RSS ascent now has heading-into-plane + 200 km target + a ×2-stretched turn. The old failure
  (`target 120 km, heading 90 ... ap 0.3 pe -6362.5`) is addressed; UNFLOWN — needs a test flight.

## Fidelity note settled 2026-08-22 (do not re-litigate)
Crew-1 flew **Crew Dragon "Resilience" = Dragon 2** (crewed variant). "Crew Dragon" IS "Crew Dragon 2";
there was never an "old crew dragon". The old first-gen **Dragon 1 / Cargo Dragon** (2010-2020) was
CARGO-ONLY, berthed by the arm, never carried crew. Our craft (`TE.18.DRAGONV2.POD` = DragonV2 = Crew
Dragon 2) is the correct Crew-1 vehicle — NO rebuild. Only check it's the **crew** Rodan (SuperDracos +
IVA screen), not the Cargo Rodan. Sourced: NASA Crew-1 pages (primary, per the no-Wikipedia rule).

## ⛔ DECISION (user, 2026-08-22): build **A first, then PSG**.
- **A = interim RSS gravity turn** (get flyable Crew-1 soonest, tune from the recorder — matches the
  project's own "interim gravity turn, PSG later, only Guide() changes" philosophy).
- **B = PSG** port (MechJebLib optimal-control ascent; FuelFlowSim is one dependency already done).

## ⛔ AUTONOMOUS FULL-MISSION BUILD 2026-08-22 (user at airport) — see docs/CREW2_BUILD_STATUS.md
Built out every remaining Crew-2 stage to match SpaceX, all RSS-gated + UNFLOWN (blocked on the ascent
reaching orbit). Verified capability event names against the Crew Rodan PARTDUMP
(`quarantine/20260728_REVERT_to_v1.0/dev_tree_before_revert/partdump_Ghidorah_9_-_Crew_Rodan.txt`):
decouplers=`decouple`, engines=`activate engine`, nosecone=`open/toggle shroud`, drogues=`deploy/cut chute`,
mains=ARMED (auto-deploy, `disarm`), dock=`undock node`. Built this session:
- **Booster droneship skips boostback** (`Landing.Droneship` flag; Crew-2 is ASDS not RTLS): flip→coast→
  entry→landing, no boostback. **Entry gate atmosphere-relative** (Kerbin 32.5 km → Earth ~65 km). 5 tests.
- **L-approach PURE module** `pure/WaypointApproach.cs` (17 tests): WP0 400 m below → WP1 220 m ahead →
  WP2 20 m, holds + GO, 200 m keep-out sphere + V-bar corridor. GLUE deliberately NOT hand-written blind
  (untestable RCS-frame) — precise integration spec in CREW2_BUILD_STATUS.md §5.
- **Phasing periapsis floor** atmosphere-relative (Kerbin 75 km → Earth ~145 km; 75 km is inside Earth's
  atmosphere). Phasing already auto-adapts to the ISS orbit.
- **Pensacola splashdown** wired (`DeorbitOps.SplashdownEarthLatDeg/LonDeg=29.8/-87.3`, [Tunable]), RSS-gated.
- All installed. Chute altitudes were already real (drogues 5486/mains 1830 m). Departure burns (4+phasing)
  and the L-approach glue are the two remaining big untestable integrations — specced, not blind-built.

## ⛔ RSS S2 LOFT + RECORDER FIELDS — BUILT 2026-08-22 after flight_0822_045358 (installed, UNFLOWN)
Pure prograde (previous entry) did NOT reach orbit - identical result. Diagnosis from the CSV: engine is
FINE (805 kN, ~240 kg/s = correct Isp), steering WAS prograde (attErr ~0.1°). The real problem: the S2
(TWR ~0.82 at ignition, from a 65 km MECO) **apexes at ~113 km - INSIDE Earth's 140 km atmosphere** -
and burning prograde AT that apex raises periapsis, not apoapsis, so the apoapsis stalls ~124 km and the
stack DESCENDS back into the air (skin 372->1751 K) and re-enters; user aborted (manual, FlightCommands).
The S2 still had ~55 t of fuel at abort - it wasn't short on dv, it was flown into the ground.
Fix: **RSS S2 aims ABOVE prograde** (`Steer`, RSS+BurnToApoapsis) by `deficit * S2LoftGainDeg` capped at
`S2MaxLoftDeg` ([Tunable] 70/45), flattening to prograde as apoapsis nears target - so the low-TWR stage
climbs OUT of the atmosphere before circularising. INTERIM heuristic, tune the two gains from flight;
this is exactly the low-TWR-upper-stage problem PSG (plan B) solves properly - if the loft won't close
orbit after a couple of gain tunes, that's the signal to start PSG.
Recorder: added **`a_aoaDeg`** (FAR aero loads) + **`a_geeForce`** (crew/structural) to the ascent block
(192 cols now). Heat was already captured (`a_maxSkinK`). assess_flight/tools read by name, unaffected.

## ⛔ RSS S2 BURNS PROGRADE — BUILT 2026-08-22 after flight_0822_040806 (installed, UNFLOWN)
That flight: ALL the separation/ignition fixes WORKED - `MECO -> booster separated by capability
(interstage decoupler) -> MVac ignition 1/3 activated 1 engine`. No breakup. But it went suborbital.
Cause (CSV): the RO M-Vac has **TWR ~0.82** (805 kN / ~100 t), and the fixed 25° pitch-law attitude
lofted the stack to a **123 km apoapsis (inside Earth's 140 km atmosphere)**, then kept burning PAST it
while pointed 25° above a now-DESCENDING velocity - thrust turned partly retrograde and BLED the orbit
(apo 123 -> 94 km), burning out ~2300 m/s short (circDv ~2400). Fix: **`Steer` burns the RSS second
stage PROGRADE** (`RssBody && BurnToApoapsis`, alongside Coast) - thrust always adds energy, apoapsis
keeps rising, the low-TWR stage chases it up out of the atmosphere. Stock keeps its pitch-law steering
(higher TWR, 2.3 km/s orbit). Prograde cuts the ~4.7 km/s of steering/gravity loss we measured. TEST:
re-fly; expect apoapsis to climb steadily to 200 km, Coast, Circularise, orbit. (User also added the
missing parachutes to the Dragon build - needed for the return splashdown.)

## ⛔ FLAMEOUT-MECO + STARVATION-GATE — BUILT 2026-08-22 after flight_0822_022834 (installed, UNFLOWN)
That flight: station targeting + azimuth WORKED (`target set to 'ISS USOS Real Size'`, `42.8 deg into
the 51.60 deg plane`). But the ascent still broke up. Cause (CSV+log): **the S1 flamed out at apo ~108 km,
just short of the 110 km MECO target**, so the guidance sat in GRAVITY TURN over a dead stage for 31 s,
and the OLD `Stage()` starvation fallback fired `ActivateNextStage()` in the gap (`staged now stage 4 …
stage 3 (no thrust)`) — decoupling the booster + lighting the MVac PREMATURELY (before ullage), which
stalled (`MVac ignition attempt … activated 0` because it was already ignited-but-dead), apoapsis decayed,
stack broke up. Fixes:
- **`Ascent.FirstStageSpent`** — GravityTurn→Meco now fires on the apoapsis target **OR** first-stage
  flameout (`!SecondStage && alt>30 km && AvailableThrust<1 kN`). Real F9 stages on depletion too. Hands
  straight to the clean `SeparateBooster` path instead of coasting a dead stage.
- **`Stage()` gated to VerticalRise/Idle only** — the starvation fallback lights the S1 off the pad and
  NEVER stages again; booster hand-off is owned by the guidance + `SeparateBooster`, the MVac by
  `IgniteSecondStage`. No more blind `ActivateNextStage` after liftoff.
- **`IgniteSecondStage` retry hardened** — only a real (re)activation spends an ignition (a stalled-but-lit
  MVac just waits for ullage); a flamed-out dud light (RO ~3%) is Shutdown→Activate re-tried within the
  3-of-4 budget. 5 new tests. TEST NEXT: same flight; expect `MECO`→`BOOSTER SEP - interstage decoupler`
  →`MVac ignition attempt 1/3` WITH thrust, orbit closed. (S1 runs ~dry at MECO — no recovery propellant
  yet; that's a later tuning of StageAltM/throttle.)

## ⛔ CAPABILITY-BASED SEPARATION + RO IGNITION — BUILT 2026-08-22 (installed, UNFLOWN)
User directive: "stop relying on the user's staging; use the parts we need by name." Root-caused both
our-autopilot separation failures to `StageManager.ActivateNextStage()` trusting the VAB staging. Fixed:
- **`AutoPilot.SeparateBooster(v)`** fires the **interstage decoupler** (`ModuleTundraDecoupler` on
  `TE.19.F9.S1.Interstage`) by CAPABILITY - the "decouple" event, action fallback (EntryOps pattern) -
  and NOTHING else. Replaces `ActivateNextStage` at the MECO stage; staging kept only as fallback if no
  interstage decoupler is found. `VehicleParts.IsInterstage` / `InterstageMarker="S1.Interstage"`.
- **`AutoPilot.IgniteSecondStage` + `IgniteSecondStageWhenSettled`** light the MVac DIRECTLY (by
  capability), and RO-correctly: wait `S2SettleBeforeIgniteS=2 s` of RCS-fore ullage, then Activate;
  if no thrust, retry up to `MaxS2IgnitionAttempts=3` (RO `ignitions=4`, so 1 in reserve;
  `ignitionReliabilityStart~0.967` → ~3% dud lights). During `BurnToApoapsis` the capability ignition
  OWNS the stage - the starvation `Stage()` fallback is bypassed so it can't fire `ActivateNextStage`
  into the ullage settle. Grounded in the resolved MVac config (`ModuleManager.ConfigCache`).
- **`StationApproach.Find`** now detects the station by **`vesselType == Station`** (rename-proof; the
  RSS ISS is `type=Station` in the save) with the stock name as priority - so LaunchAzimuth aims into
  the ISS plane and the rendezvous has a target. (Prior launch didn't target the station at all.)
- ⚠ S1 LAUNCH IGNITION UNCHANGED (still Stage()->ActivateNextStage at VerticalRise): the RO
  "ignite -> spool -> confirm thrust -> release clamps" technique is NOT yet built (next).
- Research captured in **docs/CREW2_RSS_RESEARCH.md** (Crew-2 timeline to the second + RO engine ground
  truth). Build green, INSTALLED (needs a full KSP restart). TEST: fly our autopilot, watch the log for
  `BOOSTER SEP - ... interstage decoupler` then `MVac ignition attempt 1/3 ... after ullage settle`, and
  `launch azimuth ~43 deg into the station's 51.60 deg plane`.

## ⛔ RSS ASCENT CONSTANTS — MEASURED from flight_0822_011349 (MechJeb, 2026-08-22)
A real MechJeb ascent of THIS F9+Crew Dragon off LC-39A was flown with the recorder on. `assess_flight.py`
can't read a non-autopilot flight, so I pulled the physics from the `a_` columns directly
(`scratchpad/extract_ascent.py`; note `a_phiPitchDeg` reads 0 with our AP off — used flight-path angle
from vert/surface speed instead). Findings:
- liftoff **~527 t** (the ~1000 t that drops at met 277 is the Ghidorah erector/clamp), TWR ~1.6.
- **max Q 31 kPa at 13 km, mach 1.6, flown at FULL thrust.** Kerbin's `MaxQKpa=20` would throttle us to
  the 35% floor right through it.
- real turn: flight-path angle 89°→**45° by 16 km**→**21° at staging** — pitches over FAST, the opposite
  of my first `ForBody` guess (×2 atmosphere scaling → 135 km denom → near-vertical loft. WRONG, reverted).
- **MECO/S1 sep at met 453**: 70 km, 2587 m/s srf / 2860 orb, **apoapsis 121 km**, inc **49.4°**→51.6
  (NE azimuth confirmed). Stack 142.8 t → drop 27.3 t (S1 dry) → **S2+Dragon 115.5 t**.
- **S2 MVac NEVER LIT** (lit=0 from 453→686); coasted ballistic, "S2 blew up on separation" dropped it to
  the **6.6 t Dragon** at met 686. ⇒ the S2 failure is a **CRAFT/engine issue, not guidance**, and the S2
  pushing a 6.6 t Dragon has ~8.9 km/s margin — so we can stage EARLY and still orbit easily.

APPLIED (pure, Kerbin byte-identical; 7 tests in `FlightTest`): `ForBody(profile, parkingAltitudeM)` now
sets Earth constants DIRECTLY (not scaled): `AltitudeM=200 km`, `StageAltM=110 km` (MECO apo, a touch
early for recovery margin), **new `PitchRefAltM=40 km`** (first-stage pitch scale, DECOUPLED from staging —
the fix for the loft; Kerbin=0 keeps its `StageAltM*gain/100` denom), `MecoAngleDeg=25°` (Earth stages
shallower than Kerbin's 40), **new `MaxQKpa=34`** (Kerbin 20). `TurnPitch` uses `PitchRefAltM` when >0;
`QThrottle(s, maxQ)` overload, 1-arg keeps Kerbin default. Glue picks `ForBody` for `Radius>1e6`.
⚠ Still INTERIM: a linear pitch law is a crude fit to a convex gravity turn; S2 law unvalidated (never flew).

## NEXT (build A)
1. ~~RSS parking altitude~~ — DONE (`ForBody` → 200 km).
2. ~~RSS pitch program~~ — DONE, then RE-DONE from flight data (decoupled `PitchRefAltM`, `MaxQKpa`, floor 25).
3. **← BLOCKER DIAGNOSED (KSP.log, 2026-08-22): S2 dies on a MechJeb AUTOSTAGE BURST, not ignition.**
   At both explosions (01:11:06, 01:17:54) MechJeb fired "Autostage: Executing next stage" in bursts
   (2 in 16 ms, then 8 in 90 ms), collapsing MECO/decouple/MVac-activation into ~one physics frame.
   First autostage cleanly separated `TE.19.F9.S1.Interstage`; the SECOND (MVac activation) is followed
   ~300 ms later by `TE.19.F9.S2.Engine Exploded!! - blast awesomeness: 0.5`. **NO overheat, NO
   collision, NO g-force, NO RealFuels ignition/stall line for the MVac** — it was destroyed at
   activation before it ever logged an ignition. So: a STAGING/separation problem (events collapsing
   together), NOT the S2 thrust/fuel config and NOT our guidance. Fixes: (a) fix the VAB staging so
   S1-decouple and MVac-ignite are DISTINCT sequential stages, MVac attached to the S2 TANK not the
   interstage, extendable nozzle (if any) deploying only after the interstage clears; (b) or just fly
   OUR autopilot — `Ascent.Guide` already GAPS these (MecoHoldS 2.5 s -> StageSep -> PostSepHoldS 3 s
   -> UllageSeconds 6 s -> MVac), exactly the protection MechJeb's burst lacked, so it may fly clean.
4. **Fly one RSS ascent under OUR autopilot and TUNE from the recorder** (scale-agnostic). What to watch
   on that first flight, and the likely tuning levers:
   - Does the ×2 turn over-loft? If apoapsis blows past 300 km (`AltitudeM*ApoapsisRunawayFactor=1.5`)
     the runaway backstop ABORTS (safe, not lost) — that's the signal the first-stage turn is too
     steep; ease it via a lower `StageAltM` scale or the Droneship `PitchGain`/`MecoAngleDeg`, NOT by
     touching `Guide()`.
   - Staging point: MECO apoapsis = scaled `StageAltM` (Droneship 70 km ×2 = 140 km). Confirm the
     first stage actually reaches it with recovery propellant left (barge landing needs the reserve).
   - `SepPeTargetM=40 km` is Kerbin-scale but re-enters on Earth too; confirm the apoapsis condition
     wins the `BurnToApoapsis` exit, not this. Flag for tune if the S2 sheds early.
   - Circularisation dv budget vs the S2/Dragon tanks (use the ported FuelFlowSim staged Δv).
   - Record with the black box ON; `assess_flight.py` reads it unchanged.
4. Then start **PSG (B)** — the optimal-control ascent that removes the tuning entirely.

## To fly the test (user actions)
- `python plugin/build.py install` puts the DLL in the RSS/RO KSP.
- Load the Crew-1 F9+Crew Dragon 2 on LC-39A with the ISS (51.6°/420 km) already in orbit as target.
- Log Data ON. Press AUTO SEQUENCE. Expect the log line `launch azimuth ~43 deg into the station's
  51.60 deg plane` and `ENGAGED - target 200 km`. Then hand me the flight data + KSP.log.

---

## 10:03 flight (flight_0822_095330) — UPFG LOFT ROOT-CAUSED & FIXED (ullage-poisoned Init)

**Symptom (data, every row read):** UPFG engaged at ULLAGE (ap 110 km) and owned S2 the whole burn, but
commanded ~**84° pitch (radial) for the entire burn**. Gravity-corrected thrust pitch from the CSV held
83–90° from MET 4930→5330. Horizontal speed rose only 2305→2724 (+420 m/s) while it burned 116→14.5 t
(~7000 m/s of Δv) straight into altitude: apoapsis ran to **2428 km**, periapsis never rose (−6083→−5880),
SECO never reached. Fingerprint in KSP.log: first UPFG line `tgo 315343s`.

**Not the target, not convergence, not the frame.** `Iy = Cross(prograde, up)` makes `iz2` come out
prograde by the double-cross identity (verified), and both the working gravity turn and UPFG feed
`SteerTo` the same world-frame vector.

**Root cause (PROVEN headless, `scratchpad/sim/UpfgSim.cs`):** the pure UPFG flies the measured engage
state (62 km, 2482 m/s, 21.7° FPA, 116.6 t) to a 199×261 km orbit even single-stepped once per tick
(pitch lays over 58°→0°). BUT if `Init` runs during ullage — when `AvailableThrust` (summed `finalThrust`
of ignited engines) reads ~1 kN — then `tu = ve·m/F` goes astronomical, `Init`'s `tgo`/`Rgrav`/`Rd`/`Vgo`
are garbage, and single-stepping never recovers: iF sticks at ~84° and lofts. Sim Case B (6 s ullage)
reproduces the flight to the degree (apo 2378 km, iF 84°); Case C (guidance thrust pinned full through
ullage) reaches orbit. That is the fix.

**Fix (installed 10:25, md5-verified source==live):**
- `AutoPilot`: activation gate now `a.AvailableThrust > UpfgMinThrustKn (200 kN)` — UPFG will not Init
  until the M-Vac is really at thrust; the loft holds prograde for the ~6 s of ullage.
- `UpfgFlyS2`: hard guard — if thrust < `UpfgMinThrustKn` on any tick, hold prograde, never `Step`
  (can't re-poison the persistent state).
- Log line now prints `pitch Ndeg` (iF above horizontal) — the one-glance health check: must lay over
  from ~58 toward 0, NOT sit near 84.
- `UpfgTest`: added a point-mass closure test (single-stepped UPFG reaches 199×261 km) + a test that an
  ullage-thrust Init yields a non-physical tgo (documents why the gate exists). 14 checks, all suites green.

**NEXT flight expectation:** UPFG pitch descends 58°→0°, apoapsis holds near 200 (not 2400), periapsis
rises to meet it, `SECO - UPFG orbit insertion complete` fires. Then walk the rest of the chain
(booster recovery → phasing → approach → dock → return), tuning each from its flight.

Spaceflight Now Crew-2 timeline (browser, primary source) confirms every `Crew2Timeline.cs` value and the
L-approach waypoints (WP0 400 m below, WP1 220 m front, WP2 20 m, KOS 200 m); already captured in
CREW2_RSS_RESEARCH.md — no code change needed there.

---

## 11:03 flight (flight_0822_105240) — ASCENT GATE CLOSED ✅ + 3 downstream fixes

**UPFG fix WORKED.** iF pitch laid over 42°→0° (not stuck 84°), apoapsis held 200 km, periapsis rose,
`SECO - UPFG orbit insertion complete` at **T+529 s** (real Crew-2 SECO-1 = T+527 s → **2 s off the
livestream**). Dragon separated clean on the Dragon decoupler at **200×179 km**, Dracos armed. The
ascent gate that blocked everything is closed.

Three issues that flight exposed, all fixed + installed (md5-verified, DLL 11:21):

1. **Booster flew RTLS boostback, not the droneship.** `BoosterRecovery.Profile` defaulted to `Rtls`;
   nothing set it to `Droneship`. Data: BOOSTBACK KILL→BOOSTBACK, predMiss stuck ~3110 km. FIX:
   `AutoPilot.Engage` sets `Profile = Droneship` on RSS bodies (before the ascent target is built).
   Skips boostback (Flip→Coast→Entry→Landing) and aims the barge downrange. Stock untouched.

2. **Launch window put us 65.8° / 7798 km BEHIND the station** ("we don't launch to rendezvous"). The
   seeds were from the MechJeb ascent (`AscentLonDeg 56.61°`); UPFG flies a far more DIRECT trajectory
   and sweeps only **12.98°** (`AscentTimeS 528.5`). The 43.6° seed error = most of the 58.5° phase
   residual. FIX: source defaults → 528.5 s / 12.98° (matches the live re-fit). **PhaseBias still 7.25;
   the residual will read ~15° now, not 58° — re-measure INSERTION TRAIL next flight and set PhaseBias.**

3. **Spent S2 flew off UNDER POWER** to 838×76 km at 5 G — M-Vac never shut down at SECO (throttle 0
   only idles it; once decoupled KSP resumed its throttle). It stole camera focus, blocked the
   quicksave, and the ascent re-engaged on that debris and aborted 3×. FIX: `SeparateSecondStage`
   shuts every `.S2.` engine down before firing the decoupler. Covers all sep paths.

**Mod-load errors (user asked):** all third-party VISUAL mods with unresolved deps — EVE clouds
(`Atmosphere.CloudsPQS.Update` NRE burst at scene change), Firefly textures, Restock blacklists,
optional-dep warnings. None touch DragonScreen/RO/RealFuels/Tundra/flight. DragonScreen: `panel armed:
39 controls live, 0 not found`.

**Insertion inclination 47.9°, target 51.6°** — azimuth/booster-MECO undershoot, a later trim (UPFG
holds plane, doesn't build it). NEXT: fly again — booster should run to the barge (measure miss),
window should place us far closer (~15° not 66°), S2 should go inert at SECO. Then the crew-triggered
RENDEZVOUS→DOCK→refuel→UNDOCK→DEORBIT chain.

---

## 11:29 flight (flight_0822_112918) — comprehensive trim pass

**Launch window now near-perfect.** With the corrected seeds it delivered AscentTime 529.4 (seed 528.5),
AscentLon 13.04 (seed 12.98), INSERTION TRAIL residual **-1.84 deg** (was -58.5). Applied:
- `PhaseBiasDeg 7.25 -> 1.84` (= -(intrinsic residual); lands the capsule ~1 km behind, not 7798 km).

**Inclination 47.9 vs 51.6 - ROOT-CAUSED and fixed in code.** The azimuth solver is correct for orbital
velocity, but UPFG's frame-agnostic Iy LOCKS the plane MECO delivers, and at MECO (~2.5 km/s) Earth's
rotation pulls the inertial azimuth ~4 deg east -> lower inc, which UPFG then holds. Added
`AscentInclinationBiasDeg = 3.7` (RSS-only): aims the solver 3.7 deg higher so MECO sets 51.6 directly.
Confirm inc reads ~51.6 next launch.

**First-stage AoA 17 deg (plume-wrap + windward heating) - eased.** `PitchRefAltM 40 -> 50 km` keeps the
command nearer the flight-path angle. Confirm AoA peak drops and MECO still makes ~110 km apoapsis.

**⛔ BOOSTER RECOVERY IS A CRAFT/RO-CONFIG ISSUE, NOT A CODE TRIM.** Definitive from KSP.log:
`*RF* part Falcon 9/Heavy Full Thrust Octoweb requested 1 TEATEB but got 0`. The booster is NOT dry
(~40 t / ~10% propellant at sep) - it is out of **TEATEB ignition fluid** for the entry-burn relight.
The pad start works (ground ignition); the first in-flight relight finds 0 TEATEB and the dead 66 t
stick breaks up at 44 km / mach 7 (S1.Tank + S1.Interstage Exploded). The S2 M-Vac ignites fine
("MVac ignition attempt 1/3 after ullage settle") because it has its own ignitor fluid. DragonScreen
cannot add propellant. **FIX IS CRAFT-SIDE: the S1 Octoweb needs onboard TEATEB (IGNITOR_RESOURCE) for
its in-flight relights - ascent + entry + landing = 3 ignitions.** Bump the ignitor tank via an RO
ModuleManager patch (or verify why it depletes to 0 before entry). Note the recorder's `b_lfFrac/oxFrac`
read 0 even at full tanks - they track STOCK LiquidFuel/Oxidizer, blind to RO RP-1/LqdOxygen; use mass
+ thrust, not those columns.

All built, tested, installed (md5 570ff78..., verified source==live).

## Booster relight - TEATEB was necessary but NOT sufficient; added active ullage

CORRECTED PHYSICS (user caught it): a falling tank does NOT settle under gravity - free fall is
weightless, propellant floats. Only a NON-gravitational force on the airframe settles it: aerodynamic
drag (weak at the ~65 km entry-burn altitude) or ACTIVE ullage. So even with the TEATEB patch, the
entry-burn Merlin would likely not light on unsettled propellant.

Added a booster ullage settle mirroring the M-Vac's: `BoosterRecovery` now, for the first
`EntryBurnUllageSettleS = 2.0 s` of the entry burn, holds the engine OFF and fires RCS forward
(`AttitudeController.Booster.UllageFore = 1.0` -> Drive's `s.Z = -1`, through the booster's own
OnFlyByWire) to settle propellant onto the engine feed, THEN lights. The plumbing already existed
(AttitudeController is shared; Booster.UllageFore works). NOT applied to the landing burn (dense air +
drag settles it, the entry burn just ran, and a hoverslam must not be delayed). Installed, md5
66571ee..., tests green.

Booster recovery now needs BOTH: the craft-side TEATEB patch (GameData/Crew2_Patches/F9_S1_TEATEB.cfg)
AND this ullage settle. Next flight confirms the entry/landing burns fire.

## L-approach terminal proximity ops — orchestrator BUILT, wired, tested, gated OFF

While the craft "Real Size" copies were being verified, built the guidance for the designated top
rendezvous-fidelity gap: the real Crew Dragon **L-approach** (R-bar → V-bar through three
station-keeping holds), replacing the straight-in DirectApproach for the RSS terminal run. The pure
profile (`pure/WaypointApproach.cs`) and its sequencing were already tested; what was missing was the
glue between it, the LVLH frame, and the RCS actuator.

**What it reuses (no new control law invented):**
- **Phase machine** = `WaypointApproach.Guide`/`StepPhase` (already 17-check tested): WP0 (400 m
  below) → Hold0 → WP1 (220 m ahead) → Hold1 → WP2 (20 m) → Hold2 → Handover, keep-out abort in every
  phase, auto-GO after `HoldS`=15 s.
- **Actuator** = `DockControl.Solve` — the SAME velocity servo the docking flies (braking curve +
  authority mixing), pointed at the current waypoint's world position instead of a port.
- **World↔vessel projection** = the measured convention from `DockingOps.FlyTo` (nose=`rt.up`,
  starboard=`rt.right`, top=`-rt.forward`).

**What is new (and headless-tested):**
- `pure/Lvlh.cs` — the station LVLH frame in plain doubles (`Project` + `OffsetToWorld`), the basis
  identical to `StationApproach.BuildState` but now pinned by a test (`LvlhTest`, 12 checks: signs of
  radial/along/cross, the removed-radial-velocity correction on an eccentric station, and an
  offset→world→project round-trip). This is the frame math the header warns "looks identical to a
  tuning problem right up until the capsule burns the wrong way", now tested in one place.
- `WaypointApproachOps.cs` — DirectApproach's lifecycle (Engage/Tick/Disengage/Reset/Complete) around
  the above. Stands down (clears `Engaged`, keeps `Complete`) at Handover like `DirectApproach.Match`
  on Done, so it can't zombie behind the docking controller.

**Wiring (`StationApproach`):** RSS-gated (`RssHere`, Earth-radius test) AND behind
`WaypointApproachOps.Enabled` (default **false**). When both true, DirectApproach still closes the
kilometre-scale gap; at the `EnvelopeM`=900 m initiation range it hands to the L-profile, which flies
the waypoints and hands to DockingOps at WP2. Owner-block added so the L-approach is a passenger-free
single owner while running. Stock and disabled builds take none of these branches — DirectApproach→Dock
is byte-for-byte the same path. Recorder gained `x_laPhase,x_laRangeM,x_laRadial,x_laAlong,x_laCross`
(LVLH geometry) so an enabled flight MEASURES the real approach.

**⛔ KNOWN OPEN ISSUE — why it ships OFF, not flown:** a straight WP0→WP1 leg grazes **~193 m** from
the station off the V-bar corridor, which `KeepOutBreached` (200 m) trips → it would ABORT mid-L. The
pure file already names the **curved keep-out-sphere transition** as the fix; the straight-line
`CommandToward` doesn't fly it. So the sequencing + frame + servo are all tested, but the terminal
geometry is not yet flyable end-to-end. Enabling `WaypointApproachOps.Enabled = true` is the next
deliberate test step AFTER the curved transition lands — not before.

Build clean (127 files), all suites green (LVLH 12, waypoint 17). Not yet installed to the game.

## Fly-around arc — the WP0→WP1 keep-out graze is FIXED (geometry no longer the blocker)

The one leg that would have aborted the L-approach mid-flight — a straight WP0 (400 m below) → WP1
(220 m ahead) chord grazing ~193 m off-corridor, inside the 200 m keep-out sphere — now flies AROUND
the sphere.

`WaypointApproach.ArcPoint(s)` traces a monotone spiral in the radial-along plane: bearing sweeps
180° (straight down, R-bar) → 90° (straight ahead, V-bar) while the radius eases 400 → 220 m. Because
`|ArcPoint(s)| == radius(s)` exactly and radius(s) is linear-monotone 400→220, **every point on the
arc sits at radius ≥ 220 m** — outside the sphere by construction, not by tuning. Two fly-through
waypoints `ToArc1`/`ToArc2` (s = 1/3, 2/3) discretise it; the sequencer advances on `Passed`
(proximity alone, ignoring speed) so the capsule scallops the arc without stopping, while the three
published stations WP0/WP1/WP2 stay full holds. No new control law: the arc points feed the same
`CommandToward` corridor-rate law and the same `DockControl` servo.

Test (`WaypointApproachTest`, now 23 checks): the arc + its chords are swept at 200 samples and never
breach the KOS (min range 220 m); a paired sanity check confirms the OLD straight chord *would* have
grazed <200 m, so the test has teeth; ToArc1→ToArc2→ToWP1 advance on Passed while moving.

Status change: `WaypointApproachOps.Enabled` stays **false**, but the reason is now narrower — the
*geometry* is proven, only the closed-loop flight behaviour (servo tracking the moving arc target, the
DirectApproach→L handover at 900 m, hold dispersions) is unmeasured. Flipping `Enabled = true` in
tuning.cfg is the deliberate first RSS validation flight; the recorder's `x_la*` columns read it back.
Build clean (127 files), all suites green. Not yet installed.

## ROOT CAUSE of the booster overheating: launch lit ALL THREE octaweb engine modes

Crew flew flight_0822_201219 and reported a reversed/"descending-engine" plume in the upper atmosphere,
booster tank heating, and asked to trace it. Flight-data-first found it.

**The failure chain (all from data, not theory):**
- KSP.log: `TE.19.F9.S1.Tank Exploded` at T+6 s after separation (during boostback), engine 8 s later.
  No FAR structural-failure message; dynamic pressure at the flip was only ~1.5 kPa — so NOT aero loads.
- CSV: stack skin temp climbed **300 → 644 K through the climb WHILE q fell 25 → 1.8 kPa**. Aero heating
  scales with ρv³ and was dropping — so the heat source was the engines, not the airflow.
- CSV: `a_enginesLit = 3` during ascent; ascent thrust **~11 900 kN** (9 Merlins make ~8 500).
- KSP.log: **`S1 IGNITION - 3 engine(s) lit`**.

**Root cause:** the Tundra octaweb (`TE_19_F9_S1_Engine`) is ONE part carrying THREE `ModuleEngines` —
`AllEngines` (ascent), `ThreeLanding` (entry burn), `CenterOnly` (landing) — and all three ship
`isEnabled = true`. `AutoPilot.IgniteFirstStage` activated *every* ModuleEngines on the booster part, so
at launch it lit all three modes at once. The two descent modules' plumes rendered during ascent (the
"descending-engine plume" the crew saw) and their `heatProduction` (196–248 each) stacked onto the
ascent module and cooked the S1 tank, which then let go at boostback.

**Fix (installed, md5 b59143af):** gate ignition to the all-engines mode —
`if (... && VehicleParts.EngineIdIsMode(es[m].engineID, VehicleParts.ModeAllEngines)) Activate()`.
`EngineIdIsMode(id, ModeAllEngines)` is `!Three && !Center`, so plain-engine (individual-Merlin)
boosters still light — this only skips the octaweb's descent modules. BoosterRecovery lights those later
when it steps the switch for the entry/landing burn. New `VehiclePartsTest` (13 checks) pins the contract:
launch lights `AllEngines`, never `ThreeLanding`/`CenterOnly`; individual/empty/null ids still light.

**Proof number for the re-fly:** `S1 IGNITION - 9 engine(s) lit` (or the octaweb's single all-mode
module count), `a_enginesLit` down from 3, ascent thrust ~8 500 kN not ~11 900, and the S1 tank skin
staying far below its limit through MECO. Needs a KSP restart to load. (Separate, not yet chased: S2 tank
+ Dragon trunk exploded ~4 min later during the S2/capsule reentry at ~709 K — likely trunk-jettison
timing, a different issue.)

## S2 tank + trunk explosions: symptom of a SUBORBITAL reentry (two upstream causes)

The S2 tank (20:18:28) and Dragon trunk (20:18:36) exploded because the WHOLE STACK reentered
suborbitally and neither part survives reentry. The vehicle never reached orbit. Two compounding causes,
both upstream:

1. **Octaweb over-thrust lofted the trajectory (the bug fixed this session).** With all three engine
   modes firing, thrust was ~11 900 kN and the ascent went nearly vertical: MECO at 51 km / mach 6.5
   with almost no horizontal velocity. Apoapsis 110 km but **periapsis −6125 km** (ballistic lob), and
   the circularisation Δv the log reports is **`circDv 5820`** — essentially a full orbital velocity.
   Even a perfect upper stage could not close that. The corrected 9-engine thrust should fly a proper
   gravity turn and leave a near-orbital periapsis (circDv of a few hundred m/s).

2. **The MVac never produced thrust.** `a_availThrustKn` = 0.00 kN for all 258 post-sep rows while
   `a_enginesLit` = 1 and throttle was commanded 1.00 — the signature of an engine that is LIT but
   STARVED of settled propellant (RealFuels `ullage = True`). Not TEATEB (craft carries TEATEB = 4;
   no "got 0" in the log). `IgniteSecondStageWhenSettled` fires RCS ullage then lights, and correctly
   does not spend ignitions on a stalled light — but the propellant never settled enough to build thrust
   on this flight.

The stack coasted from 110 km, fell back, S2 separated at apoapsis (mass 116 → 10 t at met 5333), and
both the S2 and the Dragon+trunk reentered ballistically (~709 K peak). The crew's `FIRE PYRD` (trunk
jettison) and `DEORBIT NOW` were **correctly refused** — periapsis was already below 25 km, i.e. already
reentering. Those guards are right, not bugs.

**Not fixing the MVac from this flight — it was already broken by the octaweb loft (and the booster was
exploding at the exact ignition moment).** The clean test is a re-fly with the octaweb fix: a proper
trajectory changes the MECO state and ullage geometry the MVac ignites in. If the MVac STILL reads
lit-but-0-thrust on a corrected ascent, THEN it is an isolated ullage-sustainment bug to fix with clean
data — not a guess layered on a broken flight.

## Re-fly flight_0822_205453: plume fix CONFIRMED; MVac ullage + grid-fin bugs found & fixed

**1. Octaweb/plume fix confirmed from data.** `S1 IGNITION - 1 engine(s) lit` (was 3); peak ascent
thrust **8257 kN** (was 11 908); `a_enginesLit = 1` (was 3); peak ascent skin **540 K** (was 644) and it
now tracks dynamic pressure instead of climbing on engine heat. MECO moved to mach 8.0 / 64 km (was
6.5 / 51 km) — a proper gravity turn. The descent-engine plumes are gone.

**2. MVac relight failure — ROOT-CAUSED and FIXED.** The stage still didn't reach orbit because the
MVac never built thrust (screenshot: "Flame-Out! Cause: No propellants / Prop Requirement Met 0.00%").
The recorder shows why: ullage `x_fore` fired 0.75 for exactly ~6 s (`UllageSeconds`) then dropped to 0
for the whole coast, while the throttle went to full. The MVac needs ~2.65 s spool and settled
propellant; when the RCS-fore cut off on the fixed clock before the engine caught, the propellant
floated and it flamed out, spending an ignition. Fix in `pure/Ascent.cs`: the `BurnToApoapsis` ullage is
now **held until `AvailableThrust ≥ UllageThrustConfirmKn` (100 kN)** — after the 6 s minimum it commands
full throttle *while keeping the RCS-fore* ("ULLAGE + LIGHT"), and only releases the settle once the
engine is really thrusting and self-settling. Gated on thrust, not a clock. 3 new FlightTest checks.

**3. Grid fins actuating while retracted — ROOT-CAUSED and FIXED.** The crew saw the stowed grid fins
deflecting; the recorder shows the booster actuators railed at ±1 (actP −1, actR/actY +1) for the whole
coast with `b_finsOut = 0` (488 rows). Cause: KSP applies the vessel's ctrlState to EVERY
`ModuleControlSurface` regardless of its deploy animation, so the retracted KRE grid fins followed the
(saturated, no-authority) coast attitude command. `DeployGridFins` only ran the deploy *animation* and
never touched the control surface. Fix in `BoosterRecovery`: new `SetGridFinControl(v, active)` sets
`ignorePitch/Yaw/Roll` on the grid-fin control surfaces — **off at recovery acquisition (stowed), on in
DeployGridFins**. Matched by the grid-fin deploy animation so it only touches grid fins.

**4. Booster entry relight — NOT ASSESSABLE this flight.** The recording ended at met 5420 (~5 min, the
MVac never fired so the flight was cut) while the booster was still coasting UP at 65–110 km, before it
ever descended to entry-burn altitude. Its relight is untested here, not failed — the octaweb fix means
it should now survive to attempt it. Re-fly (which should now reach orbit) will show it.

Installed, md5 51254c48. Needs a KSP restart.

## Live ullage tracking — the flight computer now READS the RealFuels ullage state

Built at the crew's request: instead of a blind timer, the flight computer reads RealFuels' live ullage
and lights only when the propellant is genuinely settled.

- `src/UllageProbe.cs` — reads `ModuleEnginesRF.ullageSet.GetUllageStability()` (0 floating .. 1 settled)
  by REFLECTION, cached per engine type (DragonScreen can't hard-reference RealFuels; stock returns -1
  and callers keep their old path). Confirmed the method name/signature from KSP-RO/RealFuels source
  (no-arg double, delegates to `ullageSim.GetPropellantStability()`). `RequiresUllage`, `Stability`,
  `Settled`, and `VesselWorst` (worst engine governs go/no-go).
- `AutoPilot.IgniteSecondStageWhenSettled` now gates the MVac light on `UllageProbe.VesselWorst(...) >=
  SettledStability` (tunable, default 0.90). It NEVER spends an ignition into floating propellant, holds
  with the RCS-fore on, and logs "MVac HOLDING for ullage - propellant N% settled". On a flight where the
  propellant IS settled from boost it lights the instant it is ready (which is how the working morning
  flights behaved). The booster relights are deliberately NOT gated (the landing burn is time-critical).
- Recorder gained `a_ullage` — the active vessel's worst live ullage stability (S1 during ascent, MVac
  during the S2 burn, booster during recovery). This is the number the whole investigation was missing.

Installed, md5 da08ccc5. **This does not by itself fix the settling** — if the propellant still reads 0%
the MVac still won't light — but it stops wasting ignitions, tells the crew EXACTLY why on the glass, and
`a_ullage` will show definitively whether the propellant ever settles, which is what we need to fix the
underlying RCS-settling problem.

### Regression note (MVac stopped lighting this afternoon)
Ruled OUT as the cause, by comparing the last working flight (112918, 11:29, MVac lit, orbit) to the
failing ones: S2 engine config, S2 fuel type/amount, S2-stack mass at ignition (116.6 t), and the
MECO/StageSep coast timing (2.5 s / 3 s) are ALL identical. RCS was on continuously in both. In the
working flight the propellant was settled at ignition (thrust ramped from the 0.075 idle); now it reads
unsettled. So it is NOT the engine or fuel change - it is the propellant not settling, and `a_ullage`
will now measure it directly on the next flight.

## Booster droneship recovery — entry burn now RESERVES landing propellant (real F9 method)

flight_0823_082234: the booster splashed at 471 m/s ("NO SOLUTION"). Root cause (crew diagnosed the fuel):
the entry burn ran on F9I's `verticalspeed > -300` cut, which is an RTLS number - an RTLS booster has
already killed its horizontal in boostback so retrograde is ~straight down and the burn is short. A
DRONESHIP booster keeps ~2.3 km/s of downrange (it must, to reach the barge ~500 km out - researched: real
Crew-2 landed ~535 km downrange, 2 burns, no boostback), so a SurfaceRetrograde burn held to -300 arrested
that horizontal too: it spent 22.8 t of ~22.7 t recovery propellant on the entry burn (nothing left to
land) AND killed the downrange so it fell 370 km short (landed 129 km out, `predMiss` ~130 km).

Fix: `Landing` now cuts a DRONESHIP entry burn the moment the recovery propellant reaches
`EntryBurnReserveFrac` (0.5, tunable) - reserving the landing burn's share. `LandingInputs.RecoveryPropFrac`
is filled by `BoosterRecovery.RecoveryPropUnits` (reads the RO RP-1/LOX resources directly, latched at
handover). Shorter entry burn = keeps the downrange = reaches the barge AND has fuel to land. RTLS is
unaffected (keeps the vertical-speed cut). 3 FlightTest checks. Also fixed this session: droneship landing
burn ignites on SURFACE speed (RTLS uses vertical). Both installed. Needs flight tuning of the 0.5 split
from the barge miss + landing residual. The droneship POSITION (31.56, -76.68, ~500 km) is correct.

## Launch window — was PHASE-based, now PLANE-based for RSS (the "wrong plane for rendezvous" fix)

Confirmed the crew's three checks:
1. STATION ORBIT: correct - ISS USOS Real Size reads inc 51.6, alt ~420 km, circular, LAN~0 (from the save).
2. BARGE: correct place - 31.56, -76.68, ~500 km downrange NE (real Crew-2 ~535 km). The booster landing
   SHORT was the fuel/entry-burn issue (fixed), not the barge position.
3. BOOSTER LZ LOOKUP: correct - it aims at the FIXED KK static coordinate (DroneshipEarthLat/LonDeg),
   NOT a named vessel; the code comment is explicit (a KK static is not in FlightGlobals). Log confirms
   "droneship STATIC ... no vessel; fixed Earth coordinate". Just keep that tunable in sync with the
   actual static placement.

ROOT of "not launching to the correct plane": `LaunchWindowOps.SecondsToWait` only ever solved the
along-track PHASE (right for Kerbin's 0.13 deg station, degenerate plane). The ISS at 51.6 deg needs the
RAAN matched, which only happens when the rotating pad crosses the station's plane. Built `pure/PlaneWindow.cs`
(SecondsToPlane: dot(Rz(omega t) r_site, n)=0, ascending node; 9-check test verifying the site lands on the
plane at t) and wired it: above `PlaneMatchMinIncDeg` (5 deg) the launch waits for the plane crossing (cap
`PlaneWindowCapS` ~1 day; the countdown time-warps to it), phase closed by the rendezvous phasing. Frame
built in world coords projected onto the spin axis; position via getPositionAtUT (world), velocity `.xzy`.
Installed. FLIGHT-VALIDATE: it should warp to the crossing, then insert at ~51.6 IN the station's plane
(RAAN match). If it launches at the wrong node (plane ~180 off), flip the `north` flag in SecondsToPlaneCrossing.

## flight_0823 fixes: plane node, booster coast tumbling; FPS assessed

1. PLANE 180 OFF (as predicted): the plane window picked the OPPOSITE node - the two crossings are the
   two nodes, RAAN 180 apart, and a due-NE azimuth here is co-directional with the station at the
   DESCENDING crossing. Flipped `north` true->false in SecondsToPlaneCrossing (measured, commented).
2. BOOSTER TUMBLING in coast: ROOT CAUSE - the "COAST - OVER THE TOP" branch (climbing) left `c.Rcs`
   default FALSE, so with engines out and grid fins stowed the booster had ZERO attitude authority and
   tumbled from the flip's residual rate (attErr 0-166 deg, actuators railed doing nothing). The booster
   DOES have cold-gas thrusters (TE2.19.F9.CGT x2), so RCS on the whole coast (both aim branches) gives
   it authority. Fixed in Landing.Guide Coast. [may still need the flip to end at a lower rate - watch.]
3. FPS: NOT exceptions (11 total, one-time) and NOT the recorder (buffers 25 rows, flushes ~every 5 s).
   Most likely the parallel-recovery TWO-VESSEL physics (booster + upper stage both full-physics under FAR
   at the 1500 km hold) plus the IVA screen render. Inherent load, not a new bug; needs profiling to
   pinpoint. Options if it bites: lower RangeMetres (but the booster then unloads), or throttle the screen
   render rate. Both fixes installed (md5 verified). Restart to load.

## RCS discipline: on only when the gimbal is gone (crew: "RCS wasted on pad/launch")

flight_0823: RCS was on for the ENTIRE powered ascent (VerticalRise/GravityTurn/BurnToApoapsis all 100%),
bleeding cold gas + monopropellant while the engine gimbal already held attitude. Cause: every RCS call in
the ascent only ever TURNED IT ON (SetGroup true) and nothing turned it off, so once raised (craft default
or ullage) it stayed on all flight. Fixed: `AutoPilot.SetAscentRcs` SETS the group on OR off each tick to
`c.Rcs || UllageFore>0.01`, and it also runs in the UPFG early-return path (which skipped the old code).
Each ascent phase now declares its need: powered phases (VerticalRise/GravityTurn/BurnToApoapsis-burn/
Circularise) = NO RCS (gimbal); unpowered holds (Meco/StageSep/Coast/Shutdown) = RCS on. Added c.Rcs=true
to Coast + Shutdown (were relying on the leftover-on state). 12 FlightTest checks pin it. Perfect control
where there's no gimbal, zero waste where there is. Installed.

## flight_0823_100646: RCS perfect; plane frame bug + booster landing fixed; downrange/barge mismatch open

WINS confirmed: RCS OFF through powered flight, on only for holds/ullage (0/625 gravity turn). Booster
COAST tumble largely gone (attErr mean 8, max 70; was 0-166). Entry burn now reserves fuel (12.9 t used,
was 22.8) and preserves downrange.

FIXED this round:
- PLANE FRAME BUG (crew: "mismatch between your coordinates and KSP's"): our orbit came out LAN 230 vs
  station 0. The plane normal crossed a WORLD position with an .xzy-SWIZZLED velocity - mixed frames.
  Switched to MechJeb's SwappedOrbitNormal `-(GetOrbitNormal().xzy)` (proven world frame). Now takes the
  SOONER of the two node crossings (both give the right plane once the normal is right) and LOGS a frame
  sanity check (|pad.n| at t ~ 0, plane tilt = station inc). Validate next flight.
- LANDING BURN starting at 48 km: my droneship SURFACE-speed ignition-altitude fix over-corrected (surface
  speed ~1.5 km/s high on the arc -> ignition alt 30-70 km -> burn lit at 48 km, throttled down, engine
  shut, could not relight at the ground -> fell in at 231 m/s). Reverted to VERTICAL speed: drag now bleeds
  the horizontal (43 m/s at the deck with the shorter entry burn), so vertical is the right measure and the
  hoverslam ignites a few hundred metres up, one clean light.

OPEN: the booster lands ~140 km downrange but the barge (KK static) is at ~500 km (real Crew-2). Our
simulated booster's energy only reaches ~140 km. Reconcile: move the KK static + DroneshipEarthLat/Lon to
~140 km downrange, OR fly a higher-energy ascent (later MECO, more downrange - costs recovery fuel). Also:
"Critical Parallax Error - multiple MapDecalVertexRemoveScatter on the same quad" is a MOD/KK config clash
at the landing site (combine the decals), not a DragonScreen issue. Installed, md5 verified.

## ========== STATE @ COMPACTION 2026-08-23 (read this first to resume) ==========
Project: recreate SpaceX Crew-2 in RSS/RO, flown entirely by the DragonScreen C# plugin (NO MechJeb/kOS).
Build/install: `cd plugin && python build.py install` (runs tests, refuses on fail, md5-verifies live==build).
Every DLL change needs a FULL KSP restart. Flight data: KSP/DragonScreen_capture/flight_*.csv (assess_flight.py);
KSP.log at the KSP root. Craft: saves/test/Ships/VAB/"Falcon 9 - Crew Dragon Real Size.craft". Station in save:
"ISS USOS Real Size" inc 51.6 / 420 km / LAN 0. Barge: KK STATIC at DroneshipEarthLat/Lon 31.559906,-76.679988.

INSTALLED & CONFIRMED WORKING (this session): octaweb lights ONLY all-engines mode (was 3 modes->descent
plumes+tank heat); S2 tank must be Cooled RP-1/LqdOxygen (Block-5 MVac needs it - was "No propellants");
UllageProbe reads RealFuels live ullage + gates MVac ignition; ullage HELD until thrust (UllageThrustConfirmKn
100); RCS DISCIPLINE done (SetAscentRcs sets on/off = c.Rcs||UllageFore>0.01; off through powered flight, on
for Meco/StageSep/Coast/Shutdown/ullage); reaches ORBIT; booster COAST attitude holds (RCS on both coast aim
branches; mean 8 deg); entry burn RESERVES landing fuel (EntryBurnReserveFrac 0.5, RecoveryPropFrac); grid
fins gated on deploy.

OPEN / VALIDATE NEXT FLIGHT:
1. PLANE WINDOW just re-fixed (was LAN 230 off - frame bug). Now uses MechJeb SwappedOrbitNormal + sooner
   node + LOGS "plane window: ... |pad.n| at t ~0, plane tilt = station inc". CHECK that log + the orbit's
   LAN vs the station's (save) match. north-node no longer guessed (takes min of both).
2. INCLINATION ~49.5 vs 51.6 (~2 deg low). AscentInclinationBiasDeg=1.5 (was 3.7 overshot, 1.5 undershot;
   plane-window launch time changes the drag). Re-tune from the reached inc.
3. BOOSTER LANDING BURN reverted to VERTICAL-speed ignition (surface-speed lit it at 48 km). Validate it
   lands with one clean light.
4. BOOSTER DOWNRANGE ~140 km but barge at ~500 km. DECISION: move KK static + DroneshipEarthLat/Lon to
   ~140 km, OR higher-energy ascent. Booster's energy only reaches ~140 km.
5. "Critical Parallax Error - multiple MapDecalVertexRemoveScatter on same quad" = MOD/KK config at the LZ
   (combine decals), NOT DragonScreen.

NOT DONE (fidelity): rendezvous is an ad-hoc Hohmann+phasing ladder, NOT the real NC/NH/NCC/coelliptic/TI
named-burn sequence; R-bar/V-bar L-approach (WaypointApproachOps + fly-around arc) BUILT+tested but gated
OFF (Enabled=false); targeted de-orbit+lifting entry EXIST (DeorbitOps->EntryOps, STRING 1C panel = targeted;
DEORBIT NOW = emergency land-anywhere) - need splashdown-accuracy validation; FPS drops = two-vessel physics
+ screen render (not a bug). Full phase-by-phase audit: docs/MISSION_ASSESSMENT_2026-08-22.md.

New tested pure files this session: pure/Lvlh.cs, pure/WaypointApproach.cs (arc), pure/Hohmann.cs,
pure/PlaneWindow.cs, src/UllageProbe.cs, src/WaypointApproachOps.cs. Tests: Lvlh/Waypoint/Hohmann/PlaneWindow/
VehicleParts + FlightTest additions (RCS-per-phase, ullage-hold, entry-reserve). ALL GREEN.
## ================================================================================
