# Resume state — 2026-08-24 (pre-compact)

## GOVERNING RULE
FULL FIDELITY to real Crew-2, always, no deviation, no asking. See memory
`crew2-full-fidelity-no-deviation`. RSS/RO = the real environment + the vehicle matches real SpaceX
numbers, so the real mission is the proof; copy it. Build hard real techniques to WORK, don't avoid them.
`cd plugin && python build.py test` (headless) / `install` (needs KSP restart). `build/assess_flight.py
"<full path>"` diagnoses a flight. Capture dir: `C:\Program Files (x86)\Steam\steamapps\common\Kerbal
Space Program\DragonScreen_capture`.

## WHAT'S BUILT + INSTALLED THIS SESSION (all need a KSP restart)
- **Coast fix** (StepCoastAim, BoosterRecovery): walks the aim Up->retrograde instead of a jump - CONFIRMED
  in flight_0824_193138 (roll 509 -> 21, smooth). DONE.
- **Ascent loft** (pure/Ascent.cs): PitchRefAltM 40->48, StageAltM 110->150 so the 2300 m/s speed cap
  binds (not the apoapsis). CONFIRMED staged at 68.5 km = real Crew-2 ~67 km. DONE.
- **Barge moved to the natural impact** (user rule: profile matches -> move barge): 32.7875, -76.6445
  (599 km / 38.2 deg, measured touchdown flight_0824_193138). Updated BoosterRecovery.Droneship
  Earth Lat/LonDeg, the KK static (KK_GroupCenter_Earth_"Of Course I Still Love You".cfg RefLat/Lon), and
  the installed tuning.reference.cfg. DONE (but see OUTSTANDING #1 - the waypoint marker).
- **Dragon maneuvers on the Draco** (real Crew-2, not the SuperDraco): rendezvous/phase-down/deorbit use
  NodeExecutor useRcs / DeorbitOps UllageFore; CapsuleRcs per-task thrustPercentage (DockPct 20 keeps
  docking unchanged, BurnPct 100 for burns); Draco given real thrust (DragonScreen.cfg x5). Refuel DROPPED
  (real Crew-2 is self-contained). UNFLOWN.
- **3->1 landing burn** (pure/Landing.cs): ignite THREE full-throttle (hard brake) -> hand to ONE at
  HandoverVsMps (60 m/s) for the touchdown. Two-phase ignition = aH (1-engine hoverslam from 60 m/s) + d3
  (full-throttle 3-engine brake) + spool + BoosterHeight. Tunables: HandoverVsMps, HandoverGapS.
- **SPOOL audit**: all main-engine burns lead for spool (launch LaunchSpoolS 3s, S2, entry, landing both
  phases). BurnExec.HalfBurnS now takes spoolS (0 for RCS). DONE.

## JUST FIXED (installed, UNFLOWN) — the 3->1 crash
flight_0824_202712: the 3->1 landing IGNITED at ~1080 m but the engines NEVER LIT (b_availThrustKn=0
through the whole landing burn) - hit the deck unlit at 237 m/s. ROOT: the two-phase closed-form d3 was
missing the INITIAL-RELIGHT DEAD-TIME. The engines are OFF through the descent, so the three-engine light
needs the full ullage-settle + pressure-build (LandingDeadTimeS ~5.4 s ~= 1300 m free-fall) BEFORE thrust
- the single-phase solver adds this via DeadTimeS, but d3 (closed-form) only added the spool. FIX:
`ign = aH + d3 + BoosterHeightM + vDown * (LandingDeadTimeS + LandingIgnitionLeadS)`. Now ignites ~1300 m
higher so the settle+spool finish above the deck. NEEDS A TEST FLIGHT to confirm it lights + lands, then
tune HandoverVsMps / the margins from b_engines + b_cmdThrottle + b_availThrustKn through the landing burn.

## OUTSTANDING USER REQUESTS (from the last message) — ALL 4 BUILT + INSTALLED 2026-08-24, UNFLOWN
1. **WAYPOINT to the new barge** — DONE. BargeWaypoint.cs no longer add-once-and-forget: FindExisting()
   returns the persisted "OCISLY droneship" marker; if its coords differ from the live droneship it is
   RemoveWaypoint'd and re-dropped at 32.7875/-76.6445 (SameCoordDeg 1e-4 = no-op when already there).
2. **PREDICTED TRAJECTORY on the map AND in flight** — DONE. Map side already existed (MapTrajectory.cs +
   ImpactPredictor.UpdateMapTrajectory). NEW: **FlightTrajectory.cs** projects the SAME cached MapPath/
   MapImpact/MapTarget onto the flight camera with screen-space GL (Hidden/Internal-Colored, ZTest Always),
   gated to flight view (map view stays MapTrajectory's). Started/Updated/Destroyed from FlightDriver.
3. **GREEN X on the barge centre** — DONE. FlightTrajectory draws MapTarget as a green X (impact = red X,
   path = red line) in flight view; the map overlay's green target crosshair already existed (3D map only).
4. **TWO trajectory profiles** — DONE. FlightDriver's map-update block now selects by descending vehicle:
   EntryOps/DeorbitOps -> CapsuleBcKgM2 + splashdown target; BoosterRecovery.Active (booster loaded) ->
   bcOverride 0 (LIVE-measured booster drag) + PadLat/PadLon (droneship). Compute is no longer gated on
   MapIsEnabled (both views consume it), throttled 2 Hz.

## STILL TO CONFIRM IN A TEST FLIGHT (all built + installed, none flown)
- The 3->1 landing dead-time fix (see "JUST FIXED" above): does the booster light 3 engines in time and land?
- The flight-view trajectory overlay: does the green barge X + red impact X + red path render correctly in
  flight view (projection, on-top, no ghost after the descent ends)? Visual check in-game.
- The moved waypoint: on the next flight the old marker should jump to 32.7875/-76.6445 (log line
  "barge waypoint moved from ... to ...").

## KEY FILES
- Landing 3->1 + hoverslam: pure/Landing.cs (EnginesFor line ~704, two-phase sizing ~843, throttle ~1121).
- Barge coords: BoosterRecovery.cs ~101; KK static cfg; tuning.reference.cfg (installed only).
- Waypoint: BargeWaypoint.cs (ticked by FlightDriver.Ensure). Reads the Droneship const.
- Impact prediction: src/ImpactPredictor.cs; BoosterRecovery.ImpactPointHoriz/PredictedMiss (~1050-1146).
- Map/flight draw: ScreenPainter.cs, pure/MapProjection.cs, pure/Pages.cs.
- Capsule drag for the capsule trajectory: EntryGuidance.CapsuleBcKgM2.
- Session log: docs/SESSION_2026-08-23.md (running). This file: the resume snapshot.
