# Session 2026-08-17 — the end-to-end mission pass. Read this after a compaction.

Installed DLL: **8f22e116d5953030154cb4b8d2781108** (2026-08-18, adaptive de-orbit point + coast-roll
mitigation - see `docs/SESSION_2026-08-18.md`). Priors: a6747a74 (de-orbit point only); 0a093f7d
(fix-plan pass, flown for the 08-18 assessment);
fae50863 (08-17 evening); 44cd8b9 (08-17 morning end-to-end - see the EVENING TUNING PASS section
below). `python plugin/build.py install`
works and is verified each time by md5 (installed == built). There is exactly ONE DragonScreen.dll in
GameData. The install is NOT the problem — when a fix "doesn't work" it is a logic bug, diagnosed from
the recorder + KSP.log, not a stale DLL. Prove it the same way: `find GameData -iname DragonScreen.dll
-exec md5sum` and grep the log for the new code path's own message.

## What the crew is actually flying
- Variant is often **"Ghidorah 9 - Crew Rodan"** (part `Rodan Command Pod`), not only the Falcon 9 -
  Crew Dragon. Docking port is KDSS-A (`captureMinRollDot = 0.5` on both halves).
- The crew **undocks MANUALLY** (KSP's own undock), which bypasses `UndockOps` entirely — so anything
  that must happen at undock has to be driven off the docked->undocked transition, not off UndockOps.
- They enabled **infinite fuel** to get through the rendezvous while it was still broken.

## Fixed and installed this session (all tested headless, ~7900 checks)
- **Docking** — dropped the LVLH WP0/WP1/WP2 "L" (it assumed a behind-and-below handover; rendezvous
  hands over ~60 m in front). Now gate->standoff->axial (`DockApproach.Select`). `DockingOps.FlyTo`
  was COASTING the last metres at ~1 m/s (Terminal ladder + `if(c.Coast)return`) which bypassed the
  servo; now `SpeedCapFor(range)` + servo every tick. **Latch fix: control from OUR port** at engage
  (`ourPort.MakeReferenceTransform()`, restored on disengage) — MechJeb REQUIRES control-from-node
  (`MechJebModuleDockingGuidance.cs:54`); our roll was the pod's, ~180° off. `DockRollSign` tunable
  in case KDSS wants ups opposed. Envelope guard: refuse docking > `DockEnvelopeM` (300 m) /
  `DockMaxRelSpeedMps` (2). AutoDock routes to the rendezvous when far.
- **Ignition** — `lastStageAt` reset in `AutoPilot.Engage` (revert-to-launch left the 2 s lockout in
  the future so it never ignited).
- **Booster-recovery handback** — control re-assert (`InputLockManager.ClearControlLocks` +
  `CameraManager.SetCameraFlight` + `MakeActive`); `RestoreRanges` (the PRE range is a loan for the
  recovery window); `HasBooster(v)` guard on the MECO stage command (a re-engage after separation was
  re-staging and stripping the S2); **auto-recover the booster on landing**
  (`GameEvents.OnVesselRecoveryRequested.Fire(booster)`) so it stops stealing the camera; **ascent
  auto-resumes** after the handback (`AutoPilot.ResumeAscent` set in `FlightDriver.OnDestroy`, acted
  on in `FlightDriver.Update`).
- **PRE is now a REQUIRED dependency** for booster recovery (README Requirements). Without it the
  1500 km range clamps ~300 km and the upper stage unloads/reboots mid-ascent.
- **Auto-refuel while docked** — `DockedRefuel` fills the capsule the whole time berthed (was only a
  top-up at undock, so a manual undock never refuelled).
- **Launch timing** — BOTH seeds were stale for the S2-does-insertion profile: `AscentTimeS`
  284.7->519.5 and **`AscentLonDeg` 16.76->56.61** (the LonDeg error = the 486 km-ahead insertion, the
  real rendezvous killer). `liftoffUt` re-stamp guarded (only on the pad) so `MeasureAtInsertion` fits
  the real ascent, not from the handback re-engage.
- **Booster roll** — north roll reference SWUNG (re-projected against the rotating aim: phiRoll
  -34°->+119°). Now every phase uses the **flip axis** (plane-of-flight normal, never degenerate).
  `BoosterRollRangeDeg` (130) tunable.
- **Phasing burns** — `NodeExecutor` threw throttle on remaining Δv WITHOUT checking alignment, so a
  small burn fired before the slew finished and pushed off-axis (3.88 m/s cmd -> 58 m/s residual ->
  backstop -> tank dry). Now holds throttle 0 until within `AlignedDeg` (3°); backstop counts burning
  not slewing.
- **De-orbit aim** — `AimDracoCrew` 295400->**221500**, a MEASURED re-fit (flight_0817_193135 railed
  liftMin -1 the whole entry, still landed along -49.5 km LONG; recipe `current - miss/AimGain` =
  295400-49500/0.67). Corrected a FALSE test/comment that claimed "shallow entry needs the LONGER
  aim" — backwards; a shallow entry has little shorten authority so needs the SHORTER aim.
- **Undock** — `UndockPush` fires on the undock transition (manual or auto): retro RCS burst away
  (holds attitude, translates along nose toward `away`, sign from geometry) until 2 m/s opening / 60 m
  clear, AND closes the shroud immediately.
- **Tuning system** — `[Tunable]` attribute (`src/pure/Tunable.cs`) + `src/Tuning.cs` loader:
  `PluginData/tuning.cfg` overrides any `[Tunable] public static`, hot-reloaded ~1 Hz, reference dump
  to `tuning.reference.cfg`. Tunables now include docking (RcsAccel/PidP/I/D/ContactFloor/Corridor*/
  DockGeometry*/DockEnvelope*/DockRollSign), booster roll, launch (AscentTimeS/AscentLonDeg/
  PhaseBiasDeg/TrailDistM), de-orbit (AimDracoCrew/AimGain), entry (CrossScaleM/DownScaleM/LeadS),
  undock push.

## 2026-08-17 EVENING TUNING PASS — flight_0817_205635..214211 (first clean end-to-end on own fuel)
That mission flew the WHOLE way on its own propellant: launch -> booster RTLS 0.0 km / 1 m/s -> orbit
-> rendezvous+dock -> auto-refuel (VERIFIED, 192.5 units at de-orbit, no manual top-up) -> undock ->
de-orbit -> DRAGON RECOVERED, landing 47 km from the LZ. Verified working this flight: auto-refuel,
booster camera handback (mission continued clean), booster roll DOWN 1515 -> 675 with the old-worst
descent phase 428 -> 93/OK. Two changes made + installed (DLL fae50863) FROM this flight's data:

Both were tried, FLOWN, and BOTH REGRESSED — reverted 2026-08-18. DLL is back to 44cd8b9 (byte-
identical to the morning baseline; the reverted source rebuilds to the same hash).

- **De-orbit aim 221500 -> 256000 → REVERTED to 221500.** 256000 flew OPEN LOOP and landed 260 km
  SHORT (worse than 221500's 47 km). The aim is NOT a monotonic lever: a LARGER aim makes the de-orbit
  burn run DEEPER (Pe -25.3 -> -28.7 km), which is STEEPER and SHORTER, and it is also orbit-energy
  scaled, so its effect differs flight to flight. 295400 landed LONG on a DIFFERENT orbit. Do not try
  to bracket this constant - it is a broken control variable. See Deorbit.AimDracoCrew.
- **Booster roll HoldRoll → REVERTED.** Pure rate damping was WORSE (total 675 -> 852, descent 93 ->
  233). During coast/descent roll actuation is ~0% while the stage rolls hundreds of deg, so AERO
  drives the roll; rate damping stops commanding roll and lets aero win. The active -flipAxis reference
  beats passive damping. Coast roll (324-373) is still unsolved - it needs more roll AUTHORITY vs the
  aero torque (measure the coast aero roll torque first), not a different reference.

## THE REAL PROBLEM (2026-08-18): the return architecture is contradictory
- The DE-ORBIT burns to a DEEP Pe floor → a STEEP, SHORT arrival.
- The ENTRY is SHORTEN-ONLY (KSP capsule can't extend below ~36 km, EntryGuidance trap 4) and flies
  OPEN LOOP every time - liftMin flat 0 - because it arrives SHORT of the margin schedule, so the
  shorten command is 0 the whole way. A shorten-only entry that arrives short can never recover.
- The de-orbit predictor is a VACUUM solve (no drag, over-predicts range), so the de-orbit cannot
  target a real arrival point.
- NET: precision is impossible with the current architecture. This is what the user means by "use the
  real crew dragon technique". The real principle: the DE-ORBIT sets a consistent entry interface and
  the ENTRY closes the loop on range. For our shorten-only capsule that means: de-orbit to deliver a
  reliably LONG ballistic arrival (shallower, deterministic Pe - NOT the range aim), then let the
  existing closed-loop shorten entry (which WORKS when it has margin - flight_0817_193135 engaged and
  shortened) walk it down to the LZ. Bank-REVERSAL cross-range from real Dragon still applies.

## THE MISS IS IN THE DE-ORBIT POINT, AND IT IS COUPLED (2026-08-18)
The landing-site search (`pure/Overflight.cs`) places the de-orbit burn using F9I's FIXED constants:
`DescentTimeS = 845 s` (F9I flights 068/070), `LagArcFrac 0.358`, `PhaseArcFrac 0.255`. These are
F9I's vehicle at F9I's orbit. Ours (120 km, full mono tank) is different, so the burn is placed at the
wrong downrange and the capsule lands short. BUT the variables are COUPLED and the data is
INCONSISTENT - I measured descent (ignition->last entry row) at 650 s on the 221500 flight (47 km
short) and 770 s on the 256000 flight (260 km short): a LONGER descent that missed MORE. Descent time,
aim, burn depth and burn point do not decouple from these three flights. `DescentTimeS` cannot just be
set to a measured number. The real fix is to make the de-orbit point ADAPTIVE - drive the burn point
from the drag-aware `ImpactPredictor` (predict where it will ACTUALLY land and iterate the point),
removing the F9I fixed-constant dependency - then the closed-loop shorten entry + bank reversals
fine-tune. This is a multi-flight calibration, NOT a one-constant change. Do NOT guess a constant here;
that is how 256000 and HoldRoll regressed.

## ADAPTIVE DE-ORBIT — INCREMENT 1 INSTALLED (DLL 907c6efe, 2026-08-18)
User approved the adaptive de-orbit direction. Building it the SAFE way (two regressions already burned
tonight): predictor first, VALIDATED before it moves anything.
- `ImpactPredictor.Predict(v, bcOverride)` overload added - integrate with an EXPLICIT ballistic
  coefficient so a DRAG-AWARE landing can be predicted BEFORE entry (in vacuum, no live drag yet).
- `EntryGuidance.CapsuleBcKgM2 = 440` [Tunable] - measured capsule bc (448 on 214211, 436 on 232723).
- `EntryOps.Engage` now LOGS the known-bc drag-aware predicted landing + its miss from target, at
  entry-sequence-engage. COMMANDS NOTHING. Next flight: compare that logged prediction to the actual
  DRAGON RECOVERED miss. If close, the predictor is trusted and INCREMENT 2 lets it drive the de-orbit
  point (`Overflight`) - replacing F9I's fixed DescentTimeS/LagArcFrac with a Trajectory.Solve search
  for the burn timing that lands on the LZ. Then the closed-loop shorten entry + bank reversals
  fine-tune. `pure/Trajectory.Solve` (RK4 drag integrator) is the corrector tool.

## 2026-08-18 — three findings from the user's flights
- **INCREMENT 1 VALIDATED.** known-bc(440) predicted 26.1 km; capsule landed 19.4 km from target (same
  direction, ~7 km apart). Vacuum de-orbit predictor said 337 km - useless. The drag-aware known-bc
  predictor is TRUSTED. INCREMENT 2 (let it drive the de-orbit point via Trajectory.Solve) is unblocked.
- **BOOSTER RCS-ROLL: user was RIGHT (confirmed, NOT yet fixed).** During coast the pitch/yaw RCS fires
  hard (|actP| 0.31, |actY| 0.37) and INDUCES roll - at t=56.5 both jets fire -0.47 and roll jumps with
  ZERO roll command; the roll axis under-damps it (|actR| 0.055) so it drifts a steady -3.2 deg/s and
  spikes to 18. Mechanism = RCS pitch/yaw cross-coupling into roll + under-damped roll. The coast fires
  that hard because it is chasing a swinging attitude command. PROPOSED FIX (next, one grounded change):
  reduce coast RCS thrash - hold a STABLE nose-up attitude through the coast (F9I steers to `up` until
  vspeed<-50, we start the guided lean earlier ~-35) so the aim stops swinging. VERIFY the coast
  guidance timing before changing. Do NOT repeat HoldRoll (reverted, made it worse).
- **PARKING ORBIT: FIXED (DLL 7027c8b1).** Root: `LaunchWindowOps.PhaseBiasDeg = 4.418 deg (~47 km)` -
  a stale F9I seed MeasureAtInsertion never re-fits - swamped the 1 km TrailDistM and made the window
  aim ~47 km behind. The window HITS what it commands (inserted 45.6 km vs commanded 47, ~1.4 km
  scatter), so PhaseBiasDeg was simply the trail. Set PhaseBiasDeg 4.418->0, TrailDistM 1000->2000
  (safe buffer vs scatter, stays behind, inside the 10 km direct-approach gate = NO phasing lap).
  Secondary: the capsule inserts on a slightly faster orbit (closing +54 m/s) and drifts/passes the
  station - orbit-match (co-circular at station SMA) would remove the drift; direct approach absorbs it
  for now.

## STILL OPEN — in order
1. **Verify parking orbit next flight** - expect insertion ~2 km behind, direct approach engages
   immediately, NO phasing lap.
2. **Booster coast RCS-roll fix** (above) - one grounded change, verify coast guidance timing first.
3. **INCREMENT 2**: adaptive de-orbit point (predictor validated; drive the burn via Trajectory.Solve).
2. **Booster COAST roll** 324-373 vs ~102 - needs more roll authority vs aero; measure coast roll
   torque. HoldRoll (rate damp) already TRIED and reverted - do not repeat.
3. ~~Docking TOP axis flagged INVERTED~~ **RETIRED 2026-08-18 by the full-mod audit
   (`docs/AUDIT_2026-08-18.md` Finding F1): FALSE POSITIVE in `assess_flight.py`'s own check.** The
   raw `x_dkDistT` trace shows clean convergence to +/-0.02 m noise for the last ~700 of 1186 docking
   rows, not inversion - a real inverted axis diverges (see the STARBOARD bug this check was built
   for), it does not converge and then coin-flip on noise. Do NOT flip `DockingOps.DockRollSign` on
   this basis; docking is working.

## Traps that cost time this session
- `assess_flight.py` RETURN section prints `alt` from `a_altAsl` — that is the leftover S2/ascent
  vehicle (~120 km), NOT the descending capsule. Ignore it; use the `r_` columns.
- The shroud is a TOGGLE whose guiName flips after Invoke, so the log "fired 'Close Shroud'" under an
  OPEN action is the POST-invoke label, not a bug. Do not "fix" it by swapping the names.
- Do not tune to a comment without checking it against the recorder — the de-orbit aim test was
  asserting a physically-backwards expectation.
