# Session 2026-08-17 — the end-to-end mission pass. Read this after a compaction.

Installed DLL when this was written: **44cd8b9f915d4d1a428f35f3a7ff288e**. `python plugin/build.py install`
works and is verified each time by md5 (installed == built). There is exactly ONE DragonScreen.dll in
GameData. The install is NOT the problem — when a fix "doesn't work" it is a logic bug, diagnosed from
the recorder + KSP.log, not a stale DLL. Prove it the same way: `find GameData -iname DragonScreen.dll
-exec md5sum` and grep the log for the new code path's own message.

## What the crew is actually flying
- Variant is often **"Ghidorah 9 - Crew Rodan"** (part `Rodan Command Pod`), not only the Falcon 9 -
  Crew Dragon. Docking port is KDSS-A (`captureMinRollDot = 0.5` on both halves).
- The crew **undocks MANUALLY** (KSP's own undock), which bypasses `UndockOps` entirely — so anything
  that must happen at undock has to be driven off the docked->undocked transition, not off UndockOps.
- They enabled **infinite fuel** to get through the rendezvous while it was still broken.

## Fixed and installed this session (all tested headless, ~7900 checks)
- **Docking** — dropped the LVLH WP0/WP1/WP2 "L" (it assumed a behind-and-below handover; rendezvous
  hands over ~60 m in front). Now gate->standoff->axial (`DockApproach.Select`). `DockingOps.FlyTo`
  was COASTING the last metres at ~1 m/s (Terminal ladder + `if(c.Coast)return`) which bypassed the
  servo; now `SpeedCapFor(range)` + servo every tick. **Latch fix: control from OUR port** at engage
  (`ourPort.MakeReferenceTransform()`, restored on disengage) — MechJeb REQUIRES control-from-node
  (`MechJebModuleDockingGuidance.cs:54`); our roll was the pod's, ~180° off. `DockRollSign` tunable
  in case KDSS wants ups opposed. Envelope guard: refuse docking > `DockEnvelopeM` (300 m) /
  `DockMaxRelSpeedMps` (2). AutoDock routes to the rendezvous when far.
- **Ignition** — `lastStageAt` reset in `AutoPilot.Engage` (revert-to-launch left the 2 s lockout in
  the future so it never ignited).
- **Booster-recovery handback** — control re-assert (`InputLockManager.ClearControlLocks` +
  `CameraManager.SetCameraFlight` + `MakeActive`); `RestoreRanges` (the PRE range is a loan for the
  recovery window); `HasBooster(v)` guard on the MECO stage command (a re-engage after separation was
  re-staging and stripping the S2); **auto-recover the booster on landing**
  (`GameEvents.OnVesselRecoveryRequested.Fire(booster)`) so it stops stealing the camera; **ascent
  auto-resumes** after the handback (`AutoPilot.ResumeAscent` set in `FlightDriver.OnDestroy`, acted
  on in `FlightDriver.Update`).
- **PRE is now a REQUIRED dependency** for booster recovery (README Requirements). Without it the
  1500 km range clamps ~300 km and the upper stage unloads/reboots mid-ascent.
- **Auto-refuel while docked** — `DockedRefuel` fills the capsule the whole time berthed (was only a
  top-up at undock, so a manual undock never refuelled).
- **Launch timing** — BOTH seeds were stale for the S2-does-insertion profile: `AscentTimeS`
  284.7->519.5 and **`AscentLonDeg` 16.76->56.61** (the LonDeg error = the 486 km-ahead insertion, the
  real rendezvous killer). `liftoffUt` re-stamp guarded (only on the pad) so `MeasureAtInsertion` fits
  the real ascent, not from the handback re-engage.
- **Booster roll** — north roll reference SWUNG (re-projected against the rotating aim: phiRoll
  -34°->+119°). Now every phase uses the **flip axis** (plane-of-flight normal, never degenerate).
  `BoosterRollRangeDeg` (130) tunable.
- **Phasing burns** — `NodeExecutor` threw throttle on remaining Δv WITHOUT checking alignment, so a
  small burn fired before the slew finished and pushed off-axis (3.88 m/s cmd -> 58 m/s residual ->
  backstop -> tank dry). Now holds throttle 0 until within `AlignedDeg` (3°); backstop counts burning
  not slewing.
- **De-orbit aim** — `AimDracoCrew` 295400->**221500**, a MEASURED re-fit (flight_0817_193135 railed
  liftMin -1 the whole entry, still landed along -49.5 km LONG; recipe `current - miss/AimGain` =
  295400-49500/0.67). Corrected a FALSE test/comment that claimed "shallow entry needs the LONGER
  aim" — backwards; a shallow entry has little shorten authority so needs the SHORTER aim.
- **Undock** — `UndockPush` fires on the undock transition (manual or auto): retro RCS burst away
  (holds attitude, translates along nose toward `away`, sign from geometry) until 2 m/s opening / 60 m
  clear, AND closes the shroud immediately.
- **Tuning system** — `[Tunable]` attribute (`src/pure/Tunable.cs`) + `src/Tuning.cs` loader:
  `PluginData/tuning.cfg` overrides any `[Tunable] public static`, hot-reloaded ~1 Hz, reference dump
  to `tuning.reference.cfg`. Tunables now include docking (RcsAccel/PidP/I/D/ContactFloor/Corridor*/
  DockGeometry*/DockEnvelope*/DockRollSign), booster roll, launch (AscentTimeS/AscentLonDeg/
  PhaseBiasDeg/TrailDistM), de-orbit (AimDracoCrew/AimGain), entry (CrossScaleM/DownScaleM/LeadS),
  undock push.

## OPEN — needs the next end-to-end flight, in order
1. **De-orbit LZ.** 221500 is ONE measured re-fit. Read the next landing's signed along miss and
   re-fit once more (`- miss/0.67`) or dial `Deorbit.AimDracoCrew` live. Root cause it cannot beat
   without help: `ImpactPredictor` is a VACUUM solve during the burn (no drag yet, always long) —
   that is why the burn drives to the −31.8 km depth floor. The entry HAS drag data; the aim is the
   lever.
2. **Entry cross-track** was −5 km (separate from the along-track aim). Look at the lateral authority
   (`EntryGuidance.CrossScaleM`) only AFTER the along-track is centred — one change at a time.
3. **Docking roll sign** — if ports sit aligned but won't latch, set `DockingOps.DockRollSign = -1`
   live and watch it grab (KDSS may want the ups opposed).
4. Verify: zero booster roll (assess "roll deg" toward F9I's 443 total), tight rendezvous with NO
   phasing lap, ascent never needs restarting, booster recovered with no camera flip-flop.

## Traps that cost time this session
- `assess_flight.py` RETURN section prints `alt` from `a_altAsl` — that is the leftover S2/ascent
  vehicle (~120 km), NOT the descending capsule. Ignore it; use the `r_` columns.
- The shroud is a TOGGLE whose guiName flips after Invoke, so the log "fired 'Close Shroud'" under an
  OPEN action is the POST-invoke label, not a bug. Do not "fix" it by swapping the names.
- Do not tune to a comment without checking it against the recorder — the de-orbit aim test was
  asserting a physically-backwards expectation.
